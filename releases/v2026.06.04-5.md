# JapanFG 分割 後始末 — 17 ドメインの localized 名 + audit route map

## 日本語

### 公開範囲

- `site/src/lib/siteIndex.mjs` の `DOMAIN_TITLES`: v2026.06.04-4 で新設した 17 ドメインの ja/zh/en 表示名を追加（site の領域名が slug 表示から正式名へ）。
- `tools/wiki_link_audit.ts` の `DOMAIN_SYSTEM_CANDIDATES`: 17 ドメインの route/system-link 候補を追加（非 gating の route 提案）。JapanFG エントリの旧 `legal-financial-licenses/INDEX` 参照を `financial-licenses/INDEX` へ修正。
- 三語 README/CHANGELOG/`releases/v2026.06.04-5.md`、discovery 再同期。

### 主要変更

- v2026.06.04-4（JapanFG 17 分割）の「次の作業」2 件を消化。corpus（wiki entry）は不変、discovery surface も entity 構造は不変（site 表示名 + audit 提案の補完）。
- DOMAIN_TITLES に megabanks=メガバンク / regional-banks=地方銀行 / life-insurers=生命保険 … 等 17×3 言語を追加。

### 検証結果

- `bun tools/release.ts --check --strict`: **EXIT=0**、entries=1473 / issues=0 / dead=0 / canonical_drift=0、domains=40、counts in sync。`siteIndex.mjs` import OK。
- 追加した route map は新ドメイン entry に新規 issue を生じない（issues=0 維持）。

### 既知の注意点 / 次の作業

- push 後 GitHub Actions build 緑を確認（site/ 表示変更）。JapanFG 17 分割の本体は v2026.06.04-4 で完了済み。

## English

### Release Scope

- `DOMAIN_TITLES` in `site/src/lib/siteIndex.mjs`: added ja/zh/en display names for the 17 domains created in v2026.06.04-4 (site now shows proper names instead of slugs).
- `DOMAIN_SYSTEM_CANDIDATES` in `tools/wiki_link_audit.ts`: added route/system-link candidates for the 17 domains (non-gating route suggestions); fixed the JapanFG entry's stale `legal-financial-licenses/INDEX` → `financial-licenses/INDEX`.
- Trilingual README/CHANGELOG/`releases/v2026.06.04-5.md`, discovery re-sync.

### Major Changes

- Cleared the two "next work" follow-ups from v2026.06.04-4 (the JapanFG 17-way split). The corpus (wiki entries) is unchanged, and the discovery surfaces' entity structure is unchanged (site display names + audit suggestions only).

### Validation Results

- `bun tools/release.ts --check --strict`: **EXIT=0**, entries=1473 / issues=0 / dead=0 / canonical_drift=0, domains=40, counts in sync. `siteIndex.mjs` imports OK.
- The added route map introduces no new issues on the new-domain entries (issues=0 held).

### Known Notes / Next Work

- After push, verify the GitHub Actions build is green (site/ display change). The JapanFG split itself was completed in v2026.06.04-4.

## 中文

### 发布范围

- `site/src/lib/siteIndex.mjs` 的 `DOMAIN_TITLES`：为 v2026.06.04-4 新建的 17 个域补 ja/zh/en 显示名（站点领域名从 slug 显示改为正式名）。
- `tools/wiki_link_audit.ts` 的 `DOMAIN_SYSTEM_CANDIDATES`：为 17 个域补 route/system-link 候选（非 gating 的 route 建议）；把 JapanFG 条目里过时的 `legal-financial-licenses/INDEX` 改为 `financial-licenses/INDEX`。
- 三语 README/CHANGELOG/`releases/v2026.06.04-5.md`、discovery 重新同步。

### 主要变更

- 消化 v2026.06.04-4（JapanFG 17 拆分）的两项「下一步」。corpus（wiki entry）不变，discovery surface 的 entity 结构不变（仅站点显示名 + audit 建议补全）。

### 验证结果

- `bun tools/release.ts --check --strict`：**EXIT=0**、entries=1473 / issues=0 / dead=0 / canonical_drift=0、domains=40、counts in sync。`siteIndex.mjs` import OK。
- 新增 route map 不在新域 entry 上产生新 issue（保持 issues=0）。

### 已知注意 / 下一步

- push 后确认 GitHub Actions build 绿（site/ 显示变更）。JapanFG 拆分本体已在 v2026.06.04-4 完成。
