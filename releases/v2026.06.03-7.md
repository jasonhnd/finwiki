# 領域再定位・JapanFG sub-INDEX・canonical_anchor Phase 0

## 日本語

### 公開範囲

- (B) `governance/INDEX.md`、`manufacturing/INDEX.md`（scope 再定位）。
- (C) JapanFG 新規 sub-INDEX 9 件 + `JapanFG/INDEX.md` 導航ブロック。
- (D) `SCHEMA.md`、`frontmatter-canonical-anchor-field-proposal.md`、mirror pair 4 件。
- 再生成された discovery surface、README / CHANGELOG / release notes。
- GitHub Release metadata。

### 主要変更

- **(B) 領域再定位**: mislabel だった 2 領域に scope/disambiguation を追加。`governance` は非営利・公益法人（一般財団 / 公益財団 / 一般社団 / NPO / 研究助成）が実体である旨を明示し、corporate governance（CGC / stewardship / board / activism / disclosure）は `finance` / `securities` / `exchanges` へ案内。`manufacturing` は manufacturer-finance（captive / vendor / export finance）が実体である旨を明示し、entity ページは `JapanFG`、ABS は `structured-finance` へ案内。file 移動なし。
- **(C) JapanFG sub-INDEX 細分**: 631 件が flat だった `JapanFG/` に、frontmatter `tags` 駆動で機構類型別 sub-INDEX 9 件を新設（`megabanks-and-fg` / `regional-banks` / `cooperative-finance` / `trust` / `insurance` / `securities-and-asset-management` / `payments-cards-leasing-finance` / `foreign-institutions` / `regulators-sro-policy`、計 609 entry を bare wikilink で分類）。`legal-financial-licenses/INDEX.md` の subdomain パターンに倣い各 sub-INDEX に Placement Rule（entity ページは `JapanFG/` に留まる）を明記。`JapanFG/INDEX.md` 冒頭に類型別導航ブロックを追加。**file は一切移動せず**（Option A）、53,615 件超の inbound link・URL・sitemap を保全。
- **(D) canonical_anchor Phase 0**: `canonical_anchor`（string、entity の単一 source-of-truth anchor への vault-root path）を `SCHEMA.md` の Optional / Legacy Fields と Canonical Key Order（`related` の直後）に追加。Saison（insurance ↔ JapanFG）と Toyota Financial Services（manufacturing ↔ JapanFG）の 2 mirror pair に pilot 設定（mirror ページが JapanFG anchor を指す）。proposal を status: active / Phase 0 adopted へ更新。Phase 1（audit tooling 拡張）は後続、`tools/*.ts` は未変更。

### 検証結果

- `bun tools/release.ts --check --strict`: EXIT=0、link audit entries=1420 / issues=0 → PASS（9 件の sub-INDEX wikilink 全 resolve、wiki 本文死リンク 0）、counts in sync、JSON / LF / duplicate id verify OK。
- corpus: md=1473→1482（+9 sub-INDEX）、entries=1411→1420、domains=23 維持。
- 並列 write subagent 3 件が各自独立ファイル域で実装し、相互越境なし。

### 既知の注意点

- 本 release は構造整理（領域再定位・sub-INDEX・schema 拡張）であり、wiki 本文への新規事実追加はない。
- sogo-shosha 親会社 7 件等 9 entity は非金融機関のため意図的に未分類（金融子会社は分類済み）。
- 後続タスク: canonical_anchor の全量 back-fill と audit tooling 対応（Phase 1+）、各領域の内容拡充、領域名そのものの改称（必要なら）。

### 次の作業

- push 後に remote HEAD、GitHub Actions、GitHub Release、公開 `JapanFG/INDEX` と新 sub-INDEX を確認。
- 後続で内容拡充・canonical_anchor 全量化・audit tooling を順次検討。

## English

### Release Scope

- (B) `governance/INDEX.md`, `manufacturing/INDEX.md` (scope rescope).
- (C) 9 new JapanFG sub-indexes + `JapanFG/INDEX.md` navigation block.
- (D) `SCHEMA.md`, `frontmatter-canonical-anchor-field-proposal.md`, 4 mirror-pair files.
- Regenerated discovery surfaces, README / CHANGELOG / release notes.
- GitHub Release metadata.

### Major Changes

- **(B) Domain rescope**: Added scope/disambiguation to the two mislabeled domains. `governance` is actually about non-profit / public-interest structures (一般財団 / 公益財団 / 一般社団 / NPO / research grants) — corporate governance (CGC / stewardship / board / activism / disclosure) is routed to `finance` / `securities` / `exchanges`. `manufacturing` is actually manufacturer-finance (captive / vendor / export finance) — entity pages routed to `JapanFG`, ABS to `structured-finance`. No files moved.
- **(C) JapanFG sub-index subdivision**: Gave the flat 631-entry `JapanFG/` domain 9 institution-type sub-indexes driven by frontmatter `tags` (`megabanks-and-fg` / `regional-banks` / `cooperative-finance` / `trust` / `insurance` / `securities-and-asset-management` / `payments-cards-leasing-finance` / `foreign-institutions` / `regulators-sro-policy`; 609 entries grouped as bare wikilinks). Each sub-index mirrors the `legal-financial-licenses/INDEX.md` subdomain pattern with a Placement Rule (entity pages stay in `JapanFG/`). Added a type-navigation block at the top of `JapanFG/INDEX.md`. **Zero file moves** (Option A), preserving 53,615+ inbound links / URLs / sitemap.
- **(D) canonical_anchor Phase 0**: Added `canonical_anchor` (string; vault-root path to an entity's single source-of-truth anchor) to `SCHEMA.md` Optional / Legacy Fields and the Canonical Key Order (right after `related`). Set it as a pilot on the Saison (insurance ↔ JapanFG) and Toyota Financial Services (manufacturing ↔ JapanFG) mirror pairs (the mirror page points to the JapanFG anchor). Updated the proposal to status: active / Phase 0 adopted. Phase 1 (audit-tool extension) is future work; `tools/*.ts` untouched.

### Validation Results

- `bun tools/release.ts --check --strict`: EXIT=0; link audit entries=1420 / issues=0 → PASS (all 9 sub-index wikilink sets resolve; zero dead links in the wiki body); counts in sync; JSON / LF / duplicate-id verify OK.
- Corpus: md=1473→1482 (+9 sub-indexes), entries=1411→1420, domains=23 held.
- 3 parallel write subagents each worked in an independent file scope with no cross-over.

### Known Notes

- This release is structural cleanup (domain rescope, sub-indexes, schema extension); no new facts were added to the wiki body.
- 9 entities (the 7 sogo-shosha parent trading houses, etc.) were intentionally left unbucketed as non-financial-institutions (their financial subsidiaries are bucketed).
- Follow-up: full canonical_anchor back-fill + audit tooling (Phase 1+), per-domain content expansion, and renaming the domains themselves if desired.

### Next Work

- After push, verify remote HEAD, GitHub Actions, GitHub Release, and the public `JapanFG/INDEX` plus the new sub-indexes.
- Sequence content expansion, full canonical_anchor adoption, and audit tooling next.

## 中文

### 发布范围

- (B) `governance/INDEX.md`、`manufacturing/INDEX.md`（scope 再定位）。
- (C) 9 个新 JapanFG sub-index + `JapanFG/INDEX.md` 导航块。
- (D) `SCHEMA.md`、`frontmatter-canonical-anchor-field-proposal.md`、4 个 mirror-pair 文件。
- 重新生成的 discovery surface、README / CHANGELOG / release notes。
- GitHub Release metadata。

### 主要变更

- **(B) 领域再定位**: 给两个 mislabel 的领域加 scope/disambiguation。`governance` 实为非营利 / 公益法人（一般财团 / 公益财团 / 一般社团 / NPO / 研究助成）—— 把 corporate governance（CGC / stewardship / board / activism / disclosure）路由到 `finance` / `securities` / `exchanges`。`manufacturing` 实为厂商金融（captive / vendor / export finance）—— entity 页路由到 `JapanFG`、ABS 路由到 `structured-finance`。不移文件。
- **(C) JapanFG sub-index 细分**: 给扁平的 631-entry `JapanFG/` 用 frontmatter `tags` 新建 9 个机构类型 sub-index（`megabanks-and-fg` / `regional-banks` / `cooperative-finance` / `trust` / `insurance` / `securities-and-asset-management` / `payments-cards-leasing-finance` / `foreign-institutions` / `regulators-sro-policy`，把 609 个 entry 以 bare wikilink 分类）。每个 sub-index 仿 `legal-financial-licenses/INDEX.md` 的 subdomain 模式并加 Placement Rule（entity 页留在 `JapanFG/`）。在 `JapanFG/INDEX.md` 顶部加类型导航块。**零文件移动**（Option A），保全 53,615+ inbound link / URL / sitemap。
- **(D) canonical_anchor Phase 0**: 把 `canonical_anchor`（string；指向 entity 单一 source-of-truth anchor 的 vault-root 路径）加入 `SCHEMA.md` 的 Optional / Legacy Fields 和 Canonical Key Order（在 `related` 之后）。在 Saison（insurance ↔ JapanFG）与 Toyota Financial Services（manufacturing ↔ JapanFG）两个 mirror pair 设 pilot（mirror 页指向 JapanFG anchor）。把 proposal 更新为 status: active / Phase 0 adopted。Phase 1（audit 工具扩展）留作后续；`tools/*.ts` 未动。

### 验证结果

- `bun tools/release.ts --check --strict`：EXIT=0；link audit entries=1420 / issues=0 → PASS（9 个 sub-index 的 wikilink 全部 resolve；wiki 正文零死链）；counts in sync；JSON / LF / duplicate id verify OK。
- corpus：md=1473→1482（+9 sub-index）、entries=1411→1420、domains=23 维持。
- 3 个并行 write subagent 各自在独立文件域工作，互不越界。

### 已知注意事项

- 本 release 是结构整理（领域再定位、sub-index、schema 扩展），未向 wiki 正文新增事实。
- 9 个 entity（7 个 sogo-shosha 母公司等）作为非金融机构故意未分类（其金融子公司已分类）。
- 后续：canonical_anchor 全量 back-fill + audit tooling（Phase 1+）、各领域内容扩充、以及（如需要）领域名本身的改称。

### 下一步

- push 后确认 remote HEAD、GitHub Actions、GitHub Release，以及公开的 `JapanFG/INDEX` 和新 sub-index。
- 依次推进内容扩充、canonical_anchor 全量化、audit tooling。
