# 開発進度ドキュメント拡充と canonical_anchor Phase 2 起動

## 日本語

### 公開範囲

- 新規 docs/（進度・規劃型、4 件）: `roadmap.md` / `backlog.md` / `decisions.md`(ADR) / `domains.md`、および `docs/README.md`（索引再編）。
- `insurance/saison-automobile-fire.md`（canonical_anchor drift 修正）。
- `README.md`、`CHANGELOG.md`、再生成された発見面、GitHub Release metadata。

### 主要変更

- **docs 進度・規劃型 4 文書**: roadmap（v5→v10 里程碑 + P1 canonical_anchor Phase 2 / P2 JapanFG / P3 内容扩充 + 不做清单）、backlog（歴史 open-items を P1/P2/P3 分類 + 🔴/🟡/🟢 状態）、decisions（5 件の ADR）、domains（23 領域現状表 + 最薄 6 領域扩充優先 + 領域境界備忘）。`docs/README.md` 索引を「参考型 / 進度・規劃型」に再編。**docs/ は排除済みで corpus / 発見面に不影響。**
- **Saison drift 解消（canonical_anchor Phase 2 起点）**: `insurance/saison-automobile-fire` の core body（Wiki route 段）に canonical anchor `JapanFG/saison-automobile-fire-insurance` への cross-link を追加。従来は本文で anchor を参照せず report-only audit が drift=1 を出していた。

### 検証結果

- `wiki_link_audit --fail-on-issues`: EXIT=0、entries_checked=1420 / entries_with_issues=0 / dead_wikilink=0 / **canonical_anchor_checked=8 / canonical_anchor_drift=1→0**。
- `bun tools/release.ts --check --strict`: EXIT=0、link audit issues=0、counts in sync、JSON / LF / duplicate id verify OK。
- docs/ 4 件は排除済みのため md 計数・`sitemap.xml` / `llms.txt` / `ai-index.json` には不計入（md は本 release note 分のみ +1）。敏感情報 0。

### 既知の注意点

- docs の進度文書（roadmap の優先級排序等）は主観判断を含むため後続で随時更新する。
- canonical_anchor Phase 2 の残タスク: 剩余 mirror pair back-fill、discovery への entity edge 出力、hard requirement 化（`docs/backlog.md` 参照）。

### 次の作業

- push 後に GitHub Actions、remote HEAD、GitHub Release を確認。
- 次は P1 の剩余 mirror pair back-fill、または P3 の `security` planned 4 件（`docs/roadmap.md` 参照）。

## English

### Release Scope

- New progress/planning docs (4): `roadmap.md` / `backlog.md` / `decisions.md` (ADR) / `domains.md`, plus `docs/README.md` (index reorg).
- `insurance/saison-automobile-fire.md` (canonical_anchor drift fix).
- `README.md`, `CHANGELOG.md`, regenerated discovery surfaces, GitHub Release metadata.

### Major Changes

- **4 progress/planning docs**: roadmap (milestones v5→v10 + P1 canonical_anchor Phase 2 / P2 JapanFG / P3 content expansion + out-of-scope), backlog (historical open-items by P1/P2/P3 + 🔴/🟡/🟢 status), decisions (5 ADRs), domains (23-domain status table + thinnest-6 expansion priority + boundary notes). Reorganized the `docs/README.md` index into "reference / progress-planning". **docs/ is excluded, so no corpus / discovery impact.**
- **Saison drift resolved (canonical_anchor Phase 2 kickoff)**: added a core-body cross-link from `insurance/saison-automobile-fire`'s Wiki-route section to its canonical anchor `JapanFG/saison-automobile-fire-insurance`. The page previously never referenced its anchor in the body, so the report-only audit showed drift=1.

### Validation Results

- `wiki_link_audit --fail-on-issues`: EXIT=0, entries_checked=1420 / issues=0 / dead=0 / **canonical_anchor_checked=8 / canonical_anchor_drift 1→0**.
- `bun tools/release.ts --check --strict`: EXIT=0, audit issues=0, counts in sync, JSON / LF / duplicate-id verify OK.
- The 4 docs files are excluded, so they don't count toward md / `sitemap.xml` / `llms.txt` / `ai-index.json` (md only +1 from this release note). 0 sensitive data.

### Known Notes

- The progress docs (e.g. roadmap priority ordering) involve subjective judgment and will be updated as priorities evolve.
- canonical_anchor Phase 2 remaining: back-fill remaining mirror pairs, emit entity edges in discovery, evaluate making the field a hard requirement (see `docs/backlog.md`).

### Next Work

- After push, verify GitHub Actions, remote HEAD, GitHub Release.
- Next: P1 remaining mirror-pair back-fill, or P3 `security` 4 planned pages (see `docs/roadmap.md`).

## 中文

### 发布范围

- 新建进度/规划文档（4 个）：`roadmap.md` / `backlog.md` / `decisions.md`(ADR) / `domains.md`，以及 `docs/README.md`（索引重整）。
- `insurance/saison-automobile-fire.md`（canonical_anchor drift 修复）。
- `README.md`、`CHANGELOG.md`、重新生成的发现面、GitHub Release metadata。

### 主要变更

- **4 个进度/规划文档**：roadmap（里程碑 v5→v10 + P1 canonical_anchor Phase 2 / P2 JapanFG / P3 内容扩充 + 不做清单）、backlog（历史 open-items 按 P1/P2/P3 + 🔴/🟡/🟢 状态）、decisions（5 条 ADR）、domains（23 领域现状表 + 最薄 6 领域扩充优先 + 边界备忘）。把 `docs/README.md` 索引重整为「参考型 / 进度·规划型」。**docs/ 已排除，不影响 corpus / 发现面。**
- **修复 Saison drift（canonical_anchor Phase 2 起点）**：在 `insurance/saison-automobile-fire` 的 core body（Wiki route 段）加指向 canonical anchor `JapanFG/saison-automobile-fire-insurance` 的 cross-link。此前该页从不在本文引用其 anchor，故 report-only audit 显示 drift=1。

### 验证结果

- `wiki_link_audit --fail-on-issues`：EXIT=0、entries_checked=1420 / issues=0 / dead=0 / **canonical_anchor_checked=8 / canonical_anchor_drift 1→0**。
- `bun tools/release.ts --check --strict`：EXIT=0、audit issues=0、counts in sync、JSON / LF / duplicate id verify OK。
- 4 个 docs 文件已排除，不计入 md / `sitemap.xml` / `llms.txt` / `ai-index.json`（md 仅因本 release note +1）。0 敏感数据。

### 已知注意事项

- 进度文档（如 roadmap 优先级排序）含主观判断，后续随优先级演进更新。
- canonical_anchor Phase 2 剩余：剩余 mirror pair back-fill、discovery 输出 entity edge、评估变 hard requirement（见 `docs/backlog.md`）。

### 下一步

- push 后确认 GitHub Actions、remote HEAD、GitHub Release。
- 下一步：P1 剩余 mirror pair back-fill，或 P3 `security` 4 个 planned 页（见 `docs/roadmap.md`）。
