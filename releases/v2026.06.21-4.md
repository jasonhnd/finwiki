# 読書体験・計画文書同期

## 日本語

### 公開範囲

- GitHub Issue #23 の human-site reading experience pass を実装しました。
- GitHub Issue #32 / #33 / #34 の planning artifact を公開リポジトリ内の `docs/` に同期しました。
- README、CHANGELOG、release notes、backlog、roadmap を同じ作業内で更新しました。
- wiki corpus entry、i18n mirror body、公開 route、API schema、AI discovery contract は #23 実装では変更していません。

### 主要変更

- Entry page の中央 reading measure、H1 wrapping、metadata chip wrapping、tag / TOC link wrapping を改善しました。
- `.prose` の paragraph / heading rhythm、blockquote spacing、long-link wrapping、table containment、source section density を調整しました。
- 390px / 320px mobile viewport で header が横にはみ出さないよう、narrow header の brand / nav / theme button behavior を明示しました。
- #32 coverage gap-map は FSA、BOJ、JSDA、JPX、LIAJ、IMAJ、leasing association、NTA などの公開 surface を should-cover comparison source として整理しました。
- #33 は static-first AI surface enrichment と MCP deferral を設計しました。
- #34 は source-required claim signal、marker proximity、report shape、false-positive control を設計しました。

### 検証結果

- `cd site && bun run build`: PASS、4435 pages built。
- Chrome DevTools Protocol layout measurement: 390px で `innerWidth=390`, `docScrollWidth=375`, `bodyScrollWidth=375`, theme button `x=331..363`。320px で `innerWidth=320`, `docScrollWidth=305`, `bodyScrollWidth=305`, theme button `x=261..293`。
- `bun run docs:audit`: PASS。
- `bun run html:check`: PASS、duplicate IDs 0。
- `bun tools/release.ts --check --strict`: PASS。
- `git diff --check`: PASS。

### 既知の注意点

- Mobile header では幅を保つため、language switch と Pagefind trigger を header から隠しています。言語別 URL と search index は削除していません。
- #32 / #33 / #34 は planning issue の成果物であり、audit / API enrichment / coverage expansion の実装は follow-up issue で扱います。
- 現 branch の GitHub publish、PR update、issue closeout evidence 連携は別途 publish 指示後に行います。

### 次の作業

- #23 / #26 / #30 / #32 / #33 / #34 の closeout evidence を publish 後に GitHub issue へ紐付けます。
- #24 INDEX-count gate、#28 fact freshness、#29 entity graph、#30 consistency audit の implementation issue を次の batch で進めます。
- #32 / #33 / #34 の build packet を小さな follow-up issue に分割します。

## English

### Release Scope

- Implemented the GitHub Issue #23 human-site reading experience pass.
- Synchronized GitHub Issue #32 / #33 / #34 planning artifacts under `docs/`.
- Updated README, CHANGELOG, release notes, backlog, and roadmap in the same work session.
- #23 does not change wiki corpus entries, i18n mirror bodies, public routes, API schema, or AI discovery contracts.

### Major Changes

- Improved entry-page reading measure, H1 wrapping, metadata chip wrapping, tag wrapping, and TOC link wrapping.
- Refined `.prose` paragraph / heading rhythm, blockquote spacing, long-link wrapping, table containment, and source-section density.
- Made narrow-header behavior explicit so 390px and 320px mobile viewports keep brand, navigation, and theme button inside the viewport.
- #32 coverage gap-map organizes public registry/statistical surfaces from FSA, BOJ, JSDA, JPX, LIAJ, IMAJ, leasing association, and NTA as should-cover comparison inputs.
- #33 designs static-first AI surface enrichment and MCP deferral.
- #34 designs source-required claim signals, marker proximity, report shape, and false-positive controls.

### Validation Results

- `cd site && bun run build`: PASS, 4435 pages built.
- Chrome DevTools Protocol layout measurement: at 390px, `innerWidth=390`, `docScrollWidth=375`, `bodyScrollWidth=375`, theme button `x=331..363`; at 320px, `innerWidth=320`, `docScrollWidth=305`, `bodyScrollWidth=305`, theme button `x=261..293`.
- `bun run docs:audit`: PASS.
- `bun run html:check`: PASS, duplicate IDs 0.
- `bun tools/release.ts --check --strict`: PASS.
- `git diff --check`: PASS.

### Known Notes

- On mobile headers, the language switcher and Pagefind trigger are hidden to preserve width. Language-specific URLs and the search index are not removed.
- #32 / #33 / #34 are planning deliverables; audit implementation, API enrichment, and coverage expansion remain follow-up issue work.
- GitHub publish, PR update, and issue closeout evidence linking for this branch will happen only after a separate publish instruction.

### Next Steps

- Link closeout evidence for #23 / #26 / #30 / #32 / #33 / #34 after publish.
- Continue #24 INDEX-count gate, #28 fact freshness, #29 entity graph, and #30 consistency audit implementation issues in the next batch.
- Split the #32 / #33 / #34 build packets into small follow-up issues.

## 中文

### 发布范围

- 已实现 GitHub Issue #23 的 human-site reading experience pass。
- 已把 GitHub Issue #32 / #33 / #34 的 planning artifact 同步到 `docs/`。
- 同一轮更新 README、CHANGELOG、release notes、backlog 与 roadmap。
- #23 不修改 wiki corpus entry、i18n mirror 正文、公开 route、API schema 或 AI discovery contract。

### 主要变更

- 改善 entry 页面正文宽度、H1 换行、metadata chip 换行、tag 换行与 TOC link 换行。
- 调整 `.prose` 的段落 / 标题节奏、blockquote spacing、长链接换行、表格 containment 与 source section 密度。
- 明确窄屏 header 行为，使 390px 与 320px mobile viewport 内仍能容纳 brand、导航和主题按钮。
- #32 coverage gap-map 将 FSA、BOJ、JSDA、JPX、LIAJ、IMAJ、leasing association、NTA 等公开 registry / statistics surface 作为 should-cover comparison 输入。
- #33 设计 static-first AI surface enrichment 与 MCP deferral。
- #34 设计 source-required claim signal、marker proximity、report shape 与 false-positive control。

### 验证结果

- `cd site && bun run build`: PASS，4435 pages built。
- Chrome DevTools Protocol layout measurement: 390px 下 `innerWidth=390`, `docScrollWidth=375`, `bodyScrollWidth=375`, theme button `x=331..363`；320px 下 `innerWidth=320`, `docScrollWidth=305`, `bodyScrollWidth=305`, theme button `x=261..293`。
- `bun run docs:audit`: PASS。
- `bun run html:check`: PASS，duplicate IDs 0。
- `bun tools/release.ts --check --strict`: PASS。
- `git diff --check`: PASS。

### 已知注意事项

- 移动端 header 为保证宽度，会隐藏 language switcher 与 Pagefind trigger；语言 URL 和搜索索引并未移除。
- #32 / #33 / #34 是 planning deliverable；audit 实现、API enrichment 和 coverage expansion 仍由后续 issue 处理。
- 当前分支的 GitHub publish、PR update 和 issue closeout evidence 关联，会在收到单独 publish 指令后执行。

### 下一步

- publish 后把 #23 / #26 / #30 / #32 / #33 / #34 的 closeout evidence 关联到 GitHub issue。
- 下一批继续 #24 INDEX-count gate、#28 fact freshness、#29 entity graph、#30 consistency audit 的实现 issue。
- 将 #32 / #33 / #34 的 build packet 拆成更小的 follow-up issue。
