# 型検査の残存指摘解消

## 日本語

### 公開範囲

- Issue #198 の Astro typecheck dependency、translation script type context、content collection schema、wikilink / responsive-table plugin cleanup、三言語 CHANGELOG、本 release note、release generator が同期する public discovery surface。
- wiki entry、`ja` / `en` mirror、translation algorithm、table / wikilink runtime behavior は変更しない。

### 主要変更

- exact dev dependency `@types/node@26.1.2` と file-level Node type reference を追加し、translation script の `process` global を正しく型付けした。
- deprecated `z.object(...).passthrough()` 2 件を等価な `z.looseObject(...)` に置換し、unknown frontmatter key を保持する従来 behavior を維持した。
- reference が存在しない `kanaCount` と HAST `hasMeaningfulFirstColumn` helper を削除し、runtime branch は変更せず unused-symbol hints を解消した。

### 検証結果

- `cd site && bun run check`: 39 files / 0 errors / 0 warnings / 0 hints。
- `bun test`: 50 tests / 174 assertions PASS。
- `cd site && bun run build`: Astro 2,969 pages PASS。全 canonical corpus / mirror が loose schema で引き続き受理された。
- `cd site && bun install --frozen-lockfile`: 276 installs / 377 packages、変更なし。`cd site && bun audit --production`: vulnerability 0。
- `bun run verify --out _site` の full canonical gate は PASS。Astro check は 39 files / 0 errors / 0 warnings / 0 hints、duplicate ID 0、Pagefind 2,968 pages、assembly 6,258 Astro files + 3,065 raw files、13 required routes、assembled route audit 6,029 unique internal URLs、generated-surface drift、`git diff --check` を全て確認した。

### 既知の注意点

- `@types/node` は dev-only dependency であり、公開 runtime bundle を変更しない。
- GitHub Release は `main` publish 時にのみ作成または更新する。この `pre` 向け修復では作成しない。

### 次の作業

- real pre-push、remote HEAD、PR checks を確認して Issue #198 に closeout evidence を記録し、maintainer review を待つ。self-merge / self-close はしない。

## English

### Release Scope

- Issue #198 Astro typecheck dependency, translation-script type context, content collection schemas, wikilink/responsive-table plugin cleanup, trilingual CHANGELOG, this release note, and public discovery surfaces synchronized by the release generator.
- Wiki entries, `ja` / `en` mirrors, translation algorithms, and table/wikilink runtime behavior are unchanged.

### Major Changes

- Added the exact dev dependency `@types/node@26.1.2` and a file-level Node type reference so the translation script receives the correct type for the `process` global.
- Replaced two deprecated `z.object(...).passthrough()` chains with equivalent `z.looseObject(...)` definitions while preserving unknown frontmatter keys.
- Removed the unreferenced `kanaCount` and HAST `hasMeaningfulFirstColumn` helpers, eliminating unused-symbol hints without changing a runtime branch.

### Validation Results

- `cd site && bun run check`: 39 files, 0 errors, 0 warnings, 0 hints.
- `bun test`: 50 tests / 174 assertions passed.
- `cd site && bun run build`: 2,969 Astro pages passed; the full canonical corpus/mirror remained accepted by the loose schemas.
- `cd site && bun install --frozen-lockfile`: 276 installs / 377 packages with no changes. `cd site && bun audit --production`: zero vulnerabilities.
- The full canonical `bun run verify --out _site` gate passed, confirming Astro check across 39 files with 0 errors / 0 warnings / 0 hints, zero duplicate IDs, 2,968 Pagefind pages, assembly of 6,258 Astro files plus 3,065 raw files, all 13 required routes, an assembled-route audit across 6,029 unique internal URLs, generated-surface drift, and `git diff --check`.

### Known Notes

- `@types/node` is a development-only dependency and does not change the public runtime bundle.
- A GitHub Release is created or updated only when publishing `main`; this `pre` repair does not create one.

### Next Steps

- Confirm the real pre-push, remote HEAD, and PR checks, attach closeout evidence to Issue #198, and wait for maintainer review. Do not self-merge or self-close.

## 中文

### 发布范围

- Issue #198 的 Astro typecheck dependency、translation script type context、content collection schema、wikilink / responsive-table plugin cleanup、三语 CHANGELOG、本 release note，以及 release generator 同步的公开 discovery surface。
- 不修改 wiki entry、`ja` / `en` mirror、translation algorithm 或 table / wikilink runtime behavior。

### 主要变更

- 增加 exact dev dependency `@types/node@26.1.2` 与 file-level Node type reference，为 translation script 的 `process` global 提供正确类型。
- 将两处 deprecated `z.object(...).passthrough()` chain 替换为等价的 `z.looseObject(...)`，继续保留 unknown frontmatter key。
- 删除没有任何引用的 `kanaCount` 与 HAST `hasMeaningfulFirstColumn` helper，在不修改 runtime branch 的情况下清除 unused-symbol hints。

### 验证结果

- `cd site && bun run check`: 39 files / 0 errors / 0 warnings / 0 hints。
- `bun test`: 50 tests / 174 assertions 通过。
- `cd site && bun run build`: Astro 2,969 pages 通过；完整 canonical corpus / mirror 继续被 loose schema 接受。
- `cd site && bun install --frozen-lockfile`: 276 installs / 377 packages，无变化。`cd site && bun audit --production`: vulnerability 0。
- 完整 canonical gate `bun run verify --out _site` 已通过，确认 Astro check 为 39 files / 0 errors / 0 warnings / 0 hints、duplicate ID 0、Pagefind 2,968 pages、assembly 6,258 Astro files + 3,065 raw files、13 个 required routes、assembled route audit 6,029 unique internal URLs、generated-surface drift 与 `git diff --check` 全部通过。

### 已知注意事项

- `@types/node` 仅为开发期 dependency，不修改公开 runtime bundle。
- GitHub Release 只在发布 `main` 时创建或更新；本次面向 `pre` 的修复不会创建。

### 下一步

- 确认真实 pre-push、remote HEAD 与 PR checks，把 closeout evidence 写入 Issue #198，并等待 maintainer review；不 self-merge / self-close。
