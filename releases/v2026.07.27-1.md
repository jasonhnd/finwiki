# 静的公開境界の固定

## 日本語

### 公開範囲

- Issue #177 の static publish assembler、focused tests、GitHub Pages / Vercel pipeline、関連する requirement / architecture / functional / QA / operations 文書、三言語 README / CHANGELOG と生成 public snapshot。
- wiki entry の事実本文と人間向け site locale は対象外です。

### 主要変更

- repo root の denylist copy を、`ai-index.json` / `api/entries/index.json` と明示 allowlist による raw public manifest へ置換しました。
- recursive delete の対象を direct child の `_site` / `_vercel_public` に限定し、危険な path、hidden Astro output、source / output symlink を delete 前に拒否します。
- `bun run publish:test` を追加し、Pages / Vercel の両 build pipeline で実行します。

### 検証結果

- `bun tools/vercel_build.ts` は link audit、strict release check、publish tests、Astro build（2,969 pages）、duplicate ID check（0）、Pagefind（2,968 pages）と assembly を通過しました。
- `_vercel_public` は Astro 6,258 files + allowlisted raw 3,059 files。11 必須 path は存在し、12 forbidden path、追加 hidden file、symlink は 0 でした。`--out .` は delete 前に exit 1 となりました。
- 最終 publish tests は 7/7。release-document、docs、generated-surface、txt-route、i18n、index-count、wiki-link、strict release、diff gate も PASS しました。

### 既知の注意点

- #177 branch は PR #196 が `pre` へ merge されるまで #187 commit に一時的に積み重ねます。AI discovery URL の意味論修正は Issue #179 の別 scope です。

### 次の作業

- PR #196 merge 後に最新 `pre` へ重ね直し、#177 PR を開いて review / CI を待ちます。self-merge / self-close は行いません。

## English

### Release Scope

- Issue #177 static publish assembler, focused tests, GitHub Pages / Vercel pipelines, related requirement / architecture / functional / QA / operations docs, the trilingual README / CHANGELOG, and generated public snapshot.
- Factual wiki-entry bodies and human-site locales are out of scope.

### Major Changes

- Replaced the repository-root denylist copy with a raw public manifest driven by `ai-index.json` / `api/entries/index.json` and explicit allowlists.
- Restricted recursive cleanup to the direct-child `_site` / `_vercel_public` outputs and reject unsafe paths, hidden Astro output, and source/output symlinks before deletion.
- Added `bun run publish:test` to both Pages and Vercel build pipelines.

### Validation Results

- `bun tools/vercel_build.ts` passed the link audit, strict release check, publish tests, Astro build (2,969 pages), duplicate-ID check (0), Pagefind (2,968 pages), and assembly.
- `_vercel_public` contains 6,258 Astro files plus 3,059 allowlisted raw files. All 11 required paths were present; 12 forbidden paths, extra hidden files, and symlinks were absent. `--out .` exited 1 before deletion.
- Final publish tests passed 7/7, together with the release-document, docs, generated-surface, txt-route, i18n, index-count, wiki-link, strict-release, and diff gates.

### Known Notes

- The #177 branch remains temporarily stacked on the #187 commit until PR #196 merges into `pre`. AI discovery URL semantics remain the separate scope of Issue #179.

### Next Steps

- Replay onto the latest `pre` after PR #196 merges, open the #177 PR, and wait for review/CI. Do not self-merge or self-close.