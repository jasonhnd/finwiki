# 表出典残件の解消と意見隔離契約

## 日本語

### 公開範囲

- Issue #240–#244 の 12 領域・488 source paths・877 件の表出典警告を bounded scope とする。
- Issue #193 の `.opinions/**` 公開境界を option 2（公開隔離契約）で確定する。
- 対応する 976 件の ja / en mirrors、三言語 release surfaces、root index / AI discovery、隔離監査ツールを同一 release unit で同期する。

### 主要変更

- 残件テーブル直前に、frontmatter の公開 URL から導いた table-scoped lead と出典 marker を付与し、所有残件を 877 件から 0 件にした。
- ja / en mirrors に同構造の lead を挿入し `source_hash` を再計算した（各 locale 1,442 current）。
- `.opinions/QUARANTINE.md` を追加し、サイト corpus 除外と GitHub 直接可視を分離した。`opinions:audit` を release gate に接続し、685 ファイルを 0 問題で通過させた。

### 検証結果

- provenance 全件残差 0。wiki audit 1,489 entries / issues 0。i18n ja / en 各 1,442 current。
- `opinions:audit` files=685 problems=0。`release:write` 後に discovery surfaces を再生成する。

### 既知の注意点

- 本 batch は table-scoped source lead による検出残差の解消が中心である。公的数値が後日変わる場合は通常の content review で再検証する。
- `.opinions/**` は引き続き GitHub 上で読める。隔離は非 corpus / 非確定事実を意味し、非公開を意味しない。

### 次の作業

- work を `pre` へ、続けて `main` へ promote する。
- production deployment と同じ main merge SHA に tag / GitHub Release `v2026.07.30-2` を作成する。
- Issues #192 / #193 / #240–#244 を exact evidence で close する。

## English

### Release Scope

- Clear Issues #240–#244 across 12 domains, 488 source paths, and 877 table-related warnings as the bounded scope.
- Close Issue #193 by adopting option 2: keep `.opinions/**` under an explicit public quarantine contract.
- Synchronize 976 ja/en mirrors, trilingual release surfaces, root index / AI discovery, and quarantine audit tooling in one release unit.

### Major Changes

- Attach table-scoped provenance leads and source markers derived from public frontmatter URLs; owned residual warnings move from 877 to 0.
- Insert matching leads into ja/en mirrors and recompute `source_hash` (1,442 current per locale).
- Add `.opinions/QUARANTINE.md` separating site-corpus exclusion from GitHub visibility, and wire `opinions:audit` into the release gate (685 files / 0 problems).

### Validation Results

- Provenance full residual 0. Wiki audit 1,489 entries / 0 issues. i18n JA/EN each 1,442 current.
- `opinions:audit` reports 685 files / 0 problems. Discovery surfaces are regenerated after `release:write`.

### Known Notes

- This batch centres on table-scoped source leads for detector residuals. Public figures that later change still need ordinary content review.
- `.opinions/**` remains readable on GitHub. Quarantine means non-corpus / non-canonical, not private.

### Next Steps

- Merge work into `pre`, then promote `pre` to `main`.
- Create tag / GitHub Release `v2026.07.30-2` on the exact production main SHA.
- Close Issues #192 / #193 / #240–#244 with exact evidence.