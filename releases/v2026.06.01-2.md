# GPT i18n 翻訳バッチ継続・883/883 進捗

## 日本語

### 概要

2026-06-01 JST の GPT/Codex i18n 継続バッチです。Sonnet ではなく GPT/Codex worker で recovery し、placeholder verify を通過した訳文だけを公開 i18n surface に追加しました。累計は zh 883 / en 883 です。

### 公開範囲

- 対象: `site/src/content/i18n/{zh,en}/`
- 作業 batch: 130 source entries, 260 language outputs
- 記録更新: `README.md`, `CHANGELOG.md`, this release note
- AI discovery / counts: `tools/release.py --write` により同期

### 主要変更

- 既存 worker outputs のうち別 worker directory に置かれていたファイルを、同名 `{base}.{lang}.md` と `.json` の対応に基づき正規 directory へ戻しました。
- w4 の `jaccs` は手動で zh/en を補完し、placeholder order 88/88 を確認しました。
- 未生成 23 entries は GPT/Codex recovery workers で再処理し、最後に `mufg.zh.md` のみ単独 recovery worker で補完しました。
- `bun scripts/commit-translate.mjs` で placeholder verify + unmask を全件再実行しました。
- 完了 worker は active FinWiki worker list に残していません。

### 検証結果

- `bun scripts/commit-translate.mjs`: ok=260, needs_review=0, missing=0.
- Cumulative i18n count: zh 883 / en 883.
- `bun run build`: passed, 4147 pages built.
- `python tools/release.py --check --strict`: passed.

### 既知の注意点

- 作業途中の早期 `commit-translate` では missing が残ったため、同名 output を戻してから最終 verify を再実行しました。
- 最終結果では `needs_review` と `missing` はどちらも 0 です。

### 次の作業

- 次の GPT/Codex batch で未翻訳分を継続します。
- `prep-parallel` が 0 jobs を返すまで同じ verify-first flow を繰り返します。

## English

### Overview

This is the 2026-06-01 JST GPT/Codex i18n continuation batch. The recovery was done with GPT/Codex workers rather than Sonnet, and only placeholder-verified translations were added to the public i18n surface. Cumulative progress is zh 883 / en 883.

### Release Scope

- Target: `site/src/content/i18n/{zh,en}/`
- Batch: 130 source entries, 260 language outputs
- Records: `README.md`, `CHANGELOG.md`, and this release note
- AI discovery / counts: synchronized via `tools/release.py --write`

### Major Changes

- Restored worker outputs that had landed in the wrong worker directory by matching exact `{base}.{lang}.md` names to each `.json`.
- Manually completed zh/en for w4 `jaccs` and confirmed placeholder order 88/88.
- Reprocessed 23 missing entries with smaller GPT/Codex recovery workers, then recovered `mufg.zh.md` as a final single-file task.
- Re-ran placeholder verification and unmasking for the full batch with `bun scripts/commit-translate.mjs`.
- Completed workers are not left in the active FinWiki worker list.

### Validation Results

- `bun scripts/commit-translate.mjs`: ok=260, needs_review=0, missing=0.
- Cumulative i18n count: zh 883 / en 883.
- `bun run build`: passed, 4147 pages built.
- `python tools/release.py --check --strict`: passed.

### Known Notes

- An early `commit-translate` during the work still had missing outputs, so exact-name outputs were restored to the correct directories and final verification was re-run.
- The final result has no `needs_review` and no `missing` outputs.

### Next Work

- Continue remaining untranslated entries with the next GPT/Codex batch.
- Repeat the same verify-first flow until `prep-parallel` reports 0 jobs.

## 中文

### 概要

这是 2026-06-01 JST 的 GPT/Codex i18n 续跑批次。本轮按用户要求使用 GPT/Codex worker，而不是 Sonnet；只把通过 placeholder verify 的译文加入公开 i18n surface。累计进度为 zh 883 / en 883。

### 发布范围

- 目标：`site/src/content/i18n/{zh,en}/`
- 批次：130 个源条目，260 个语言输出
- 记录更新：`README.md`、`CHANGELOG.md` 和本 release note
- AI discovery / counts：通过 `tools/release.py --write` 同步

### 主要变更

- 对落在错误 worker 目录里的译文，按精确 `{base}.{lang}.md` 文件名和 `.json` 所在目录重新归位。
- 手动补齐 w4 的 `jaccs` 中英译文，并确认 placeholder 顺序 88/88 一致。
- 对 23 个未生成条目启动更小粒度的 GPT/Codex recovery worker，最后单独补齐 `mufg.zh.md`。
- 使用 `bun scripts/commit-translate.mjs` 重新执行整批 placeholder verify 和 unmask。
- 完成的 worker 没有留在 active FinWiki worker list。

### 验证结果

- `bun scripts/commit-translate.mjs`：ok=260，needs_review=0，missing=0。
- 累计 i18n 数量：zh 883 / en 883。
- `bun run build`：通过，构建 4147 页。
- `python tools/release.py --check --strict`：通过。

### 已知注意事项

- 工作中途过早执行过一次 `commit-translate`，当时仍有 missing；随后已按文件名归位输出，并重新执行最终校验。
- 最终结果中 `needs_review` 和 `missing` 都为 0。

### 下一步

- 下一轮继续用 GPT/Codex 跑剩余未翻译条目。
- 持续重复 verify-first flow，直到 `prep-parallel` 返回 0 jobs。
