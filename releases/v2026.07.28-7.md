# 翻訳対象コーパスの統一

## 日本語

### 公開範囲

- Issue #182 の translation source discovery、preparation / manifest / smoke tooling、focused regression test、三言語 README / CHANGELOG、本 release note、release generator が同期する public discovery surface。
- source entry と committed `ja` / `en` mirror は変更しない。

### 主要変更

- 23 directory の固定 list を削除し、`i18n:status` と同じ canonical public-corpus walker で 40 domain の current entry を自動発見する。
- direct translation、parallel / interactive prep、manifest-driven prep を同じ source set に揃えた。新しい public domain は second list の編集なしで対象になる。
- write-free `--dry-run`、denominator parity / future-domain regression tests、現在 path を使う smoke target を追加した。

### 検証結果

- `bun test site/scripts/corpus-roots.test.mjs site/scripts/protect.test.mjs`: 16 tests / 59 assertions PASS。
- `bun run i18n:status`: source 1,442、ja / en 各 current 1,442、stale / orphaned / missing 0、fidelity ok 1,442。
- `bun site/scripts/prep-translate.mjs --force --dry-run`: write-free で 1,442 jobs を発見。
- `bun site/scripts/emit-masked.mjs`: current smoke target から 56 / 46 masks を生成。
- `bun run verify --out _site`: 82 tests / 272 assertions、Astro check 44 files / 0 errors / 0 warnings / 0 hints、2,969 pages、entry metadata issues 0、最終 5,864 routes / 6,036 generated URLs、production vulnerability 0 で PASS。

### 既知の注意点

- discovery と preparation tooling の修復であり、自動翻訳 API を呼ばず、既存 mirror を書き換えない。
- GitHub Release は `main` publish 時にのみ作成または更新する。この `pre` 向け修復では作成しない。

### 次の作業

- full canonical gate、real pre-push、remote HEAD、PR checks を確認して Issue #182 に closeout evidence を記録し、maintainer review を待つ。self-close はしない。

## English

### Release Scope

- Issue #182 translation source discovery, preparation/manifest/smoke tooling, focused regression tests, the trilingual README/CHANGELOG, this release note, and public discovery surfaces synchronized by the release generator.
- Source entries and committed `ja` / `en` mirrors are unchanged.

### Major Changes

- Removed the fixed 23-directory list and now discover current entries across all 40 domains with the same canonical public-corpus walker used by `i18n:status`.
- Aligned direct translation, parallel/interactive preparation, and manifest-driven preparation on the same source set. A new public domain no longer requires editing a second list.
- Added a write-free `--dry-run`, denominator-parity and future-domain regression tests, and smoke targets that use current paths.

### Validation Results

- `bun test site/scripts/corpus-roots.test.mjs site/scripts/protect.test.mjs`: 16 tests / 59 assertions passed.
- `bun run i18n:status`: 1,442 sources; ja/en each have 1,442 current, zero stale/orphaned/missing, and 1,442 `fidelity: ok`.
- `bun site/scripts/prep-translate.mjs --force --dry-run`: found 1,442 jobs without writing files.
- `bun site/scripts/emit-masked.mjs`: generated 56 / 46 masks from the current smoke targets.
- `bun run verify --out _site`: passed with 82 tests / 272 assertions, Astro check across 44 files with 0 errors / 0 warnings / 0 hints, 2,969 pages, zero entry-metadata issues, final audits across 5,864 routes / 6,036 generated URLs, and zero production vulnerabilities.

### Known Notes

- This repairs discovery and preparation tooling without calling an automatic-translation API or rewriting an existing mirror.
- A GitHub Release is created or updated only when publishing `main`; this `pre` repair does not create one.

### Next Steps

- Confirm the full canonical gate, real pre-push, remote HEAD, and PR checks; attach closeout evidence to Issue #182; and wait for maintainer review. Do not self-close.