# 決済・カード実体ページ深化 Wave A

## 日本語

### 公開範囲

- GitHub Issue #26 の Wave A として、`payment-firms/` と `card-issuers/` の既存 8 実体ページを深化しました。
- 対象は PayPay、メルペイ、SB ペイメントサービス、Wise Payments Japan、ネットプロテクションズホールディングス、三井住友カード、PayPay カード、セブン・カードサービスです。
- 新規 public URL は追加せず、既存ページの business model、規制上の位置づけ、group / product boundary を公開情報に基づいて補強しました。
- ja / en / zh の i18n mirror 24 件を source hash と本文差分に合わせて同期しました。

### 主要変更

- 決済会社 5 件に、資金移動、前払式支払手段、カード番号取扱契約締結、包括信用購入あっせん、PSP / BNPL / overseas-remittance などの boundary を整理しました。
- カード発行会社 3 件に、issuer、wallet、retail finance、nanaco / PayPay ecosystem、SMFG platform との関係を追加しました。
- 各ページの `last_updated` / `last_tended` を 2026-06-20 に更新し、公式会社情報、IR、サービス説明、登録情報に基づく sources を補強しました。
- i18n mirror は ja / en / zh で source_hash を更新し、加筆した section を各言語に反映しました。
- README、CHANGELOG、root homepage、AI discovery surface を同じ作業内で同期します。

### 検証結果

- `bun run i18n:status`: ja / zh / en とも `current=1436`, `stale=0`, `missing=0`, `orphaned=0`。
- `bun tools/wiki_link_audit.ts --fail-on-issues`: `entries_checked=1483`, `entries_with_issues=0`, `dead_wikilink_references=0`, `dead_wikilink_targets=0`, `canonical_anchor_drift=0`。
- `bun tools/release.ts --write`: `markdown_files=1566`, `public_pages=1565`, `sitemap_urls=1566`, `domains=40`, `link_audited_entries=1483`, `api_entries=1476` を生成し、README / root `index.html` / AI discovery surface を同期。
- `bun tools/release.ts --check --strict`: counts in sync、JSON / LF / duplicate-id verify OK。
- `git diff --check`: EXIT=0。

### 既知の注意点

- 本作業は既存ページの深化であり、domain count や URL structure は変更しません。
- 定量値や登録番号は公式ページ・規制当局資料で確認できる範囲に限定し、確認不能な推測は追加していません。
- GitHub Release の公開と issue close は、`origin/main` への publish 指示が出た後に実施します。

### 次の作業

- #26 の後続 wave では、#22 task packet の banks / foreign branches、capital-markets / finance arms、insurance / trust / manufacturer anchors を順に扱います。
- 次 wave でも公式情報を再確認し、i18n mirror と discovery surface を同時に同期します。

## English

### Release Scope

- Deepened 8 existing entity pages under `payment-firms/` and `card-issuers/` as GitHub Issue #26 Wave A.
- Covered PayPay, Merpay, SB Payment Service, Wise Payments Japan, Net Protections Holdings, Sumitomo Mitsui Card, PayPay Card, and Seven Card Service.
- No new public URLs were added. Existing pages were strengthened with public-source business-model, regulatory-positioning, and group / product-boundary context.
- Synced 24 ja / en / zh i18n mirrors to the updated source hashes and edited sections.

### Major Changes

- Added boundaries for funds transfer, prepaid payment instruments, card-number contract handling, comprehensive credit purchase intermediation, PSP, BNPL, and overseas-remittance roles across the five payment-company pages.
- Added issuer, wallet, retail-finance, nanaco / PayPay ecosystem, and SMFG-platform context across the three card-issuer pages.
- Updated `last_updated` / `last_tended` to 2026-06-20 and strengthened sources with official company, IR, service, and registration materials.
- Updated i18n mirror `source_hash` values and translated the newly added sections in ja / en / zh.
- README, CHANGELOG, the root homepage, and AI discovery surfaces are synchronized in the same work session.

### Validation Results

- `bun run i18n:status`: ja / zh / en all report `current=1436`, `stale=0`, `missing=0`, `orphaned=0`.
- `bun tools/wiki_link_audit.ts --fail-on-issues`: `entries_checked=1483`, `entries_with_issues=0`, `dead_wikilink_references=0`, `dead_wikilink_targets=0`, `canonical_anchor_drift=0`.
- `bun tools/release.ts --write`: generated `markdown_files=1566`, `public_pages=1565`, `sitemap_urls=1566`, `domains=40`, `link_audited_entries=1483`, `api_entries=1476`; README / root `index.html` / AI discovery surfaces synchronized.
- `bun tools/release.ts --check --strict`: counts in sync; JSON / LF / duplicate-id verification OK.
- `git diff --check`: EXIT=0.

### Known Notes

- This is an existing-page deepening pass. Domain count and URL structure do not change.
- Quantitative facts and registration numbers are limited to what could be checked against official pages or regulator materials.
- GitHub Release publication and issue closeout will be done only after an explicit `origin/main` publish instruction.

### Next Steps

- Later #26 waves should continue through the #22 task packet: banks / foreign branches, capital-markets / finance arms, and insurance / trust / manufacturer anchors.
- Each later wave should re-check official sources and sync i18n mirrors plus discovery surfaces in the same work session.

## 中文

### 发布范围

- 按 GitHub Issue #26 的 Wave A，深化 `payment-firms/` 与 `card-issuers/` 下 8 个既有实体页。
- 覆盖 PayPay、Merpay、SB Payment Service、Wise Payments Japan、Net Protections Holdings、Sumitomo Mitsui Card、PayPay Card、Seven Card Service。
- 不新增公开 URL，只在既有页面中补强 business model、监管定位、集团关系与产品边界。
- 同步 24 个 ja / en / zh i18n mirror，使 source hash 与新增正文一致。

### 主要变更

- 在 5 个支付公司页面中补充资金移转、预付式支付工具、卡号合同签约、综合信用购买中介、PSP、BNPL、跨境汇款等边界。
- 在 3 个发卡公司页面中补充 issuer、wallet、retail finance、nanaco / PayPay ecosystem、SMFG platform 的关系。
- 把各页 `last_updated` / `last_tended` 更新到 2026-06-20，并用官方公司信息、IR、服务说明与登记资料补强 sources。
- 更新 i18n mirror 的 `source_hash`，并把新增 section 翻译到 ja / en / zh。
- README、CHANGELOG、根首页和 AI discovery surface 在同一轮工作中同步。

### 验证结果

- `bun run i18n:status`：ja / zh / en 均为 `current=1436`、`stale=0`、`missing=0`、`orphaned=0`。
- `bun tools/wiki_link_audit.ts --fail-on-issues`：`entries_checked=1483`、`entries_with_issues=0`、`dead_wikilink_references=0`、`dead_wikilink_targets=0`、`canonical_anchor_drift=0`。
- `bun tools/release.ts --write`：生成 `markdown_files=1566`、`public_pages=1565`、`sitemap_urls=1566`、`domains=40`、`link_audited_entries=1483`、`api_entries=1476`，并同步 README / 根 `index.html` / AI discovery surface。
- `bun tools/release.ts --check --strict`：counts in sync，JSON / LF / duplicate-id verify OK。
- `git diff --check`：EXIT=0。

### 已知注意事项

- 本次是既有页面深化，不改变 domain count 或 URL structure。
- 定量事实与登记编号只写入可由官方页面或监管资料确认的范围，不加入无法核验的推测。
- GitHub Release 发布与 issue close 只在收到明确 `origin/main` publish 指示后执行。

### 下一步

- #26 后续 wave 按 #22 task packet 继续处理 banks / foreign branches、capital-markets / finance arms、insurance / trust / manufacturer anchors。
- 后续 wave 仍需复核官方来源，并同步 i18n mirror 与 discovery surface。
