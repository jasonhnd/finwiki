# 正準アンカー契約と文書入口の統一

## 日本語

### 公開範囲

- Issue #190 の `canonical_anchor` 現行契約、mirror policy、cross-domain anchor convention、開発文書入口、三言語 README / CHANGELOG、本 release note、release generator が同期する public discovery surface。
- wiki entry の事実本文、`ja` / `en` translation mirror、domain map、公開 URL は変更しない。

### 主要変更

- `canonical_anchor` は真の mirror page に必須、canonical anchor と通常の related page では省略、declared target の解決・core-body link・drift zero は release gate、semantic mirror 判定と reverse link は editorial review、という現行契約に root schema、architecture docs、policy pages を統一した。
- 実装前 proposal を historical decision record として明示し、現行 authority を `SCHEMA.md`、ADR-007、entry-authoring checklist へ集約した。
- `docs/README.md` に未掲載だった 11 active developer docs を追加し、唯一の開発文書入口から全 active docs を辿れる状態にした。JapanFG split 前の現行表現も role-specific institution domains へ更新した。

### 検証結果

- `bun run docs:audit`: PASS。
- `bun run docs:stale`: 66 active docs / 16 checks、stale reference 0 で PASS。
- `git diff --check`: PASS。
- `bun run verify --out _site`: 82 tests / 272 assertions、Astro check 44 files / 0 errors / 0 warnings / 0 hints、Astro 2,969 pages、entry metadata 2,884 / issues 0、final HTML audit 5,864 routes、generated URL audit 6,037 unique URLs、production vulnerability 0 で PASS。
- generated-surface drift は API 1,482 entries と fixed-timestamp byte identity を確認し、docs / audit artifact / local path leakage 0 で PASS。

### 既知の注意点

- tooling は宣言済み `canonical_anchor` の機械的 drift を fail-close するが、ページが意味的に mirror かどうか、および anchor から mirror への reverse link は editorial review の責任である。
- 本変更は documentation-contract repair であり、既存 entry の canonical designation や公開事実を再分類しない。
- GitHub Release は `main` publish 時にのみ作成または更新する。この `pre` 向け修復では作成しない。

### 次の作業

- real pre-push、remote HEAD、PR checks を確認して Issue #190 に closeout evidence を記録し、maintainer review を待つ。Issue は self-close しない。

## English

### Release Scope

- Issue #190's current `canonical_anchor` contract, mirror policy, cross-domain anchor convention, developer-documentation entry point, the trilingual README/CHANGELOG, this release note, and public discovery surfaces synchronized by the release generator.
- Factual wiki-entry bodies, `ja` / `en` translation mirrors, the domain map, and public URLs are unchanged.

### Major Changes

- Aligned the root schema, architecture documentation, and policy pages on the current contract: `canonical_anchor` is required on true mirrors, omitted from anchors and ordinary related pages, declared target resolution/core-body linkage/zero drift are release-gated, and semantic mirror classification plus reverse links remain editorial review.
- Marked the pre-implementation proposal as a historical decision record and consolidated current authority in `SCHEMA.md`, ADR-007, and the entry-authoring checklist.
- Added the 11 previously unlisted active developer documents to `docs/README.md`, making every active document reachable from the sole developer-documentation entry point. Current wording left over from the JapanFG split was updated to role-specific institution domains.

### Validation Results

- `bun run docs:audit`: passed.
- `bun run docs:stale`: passed across 66 active documents / 16 checks with zero stale references.
- `git diff --check`: passed.
- `bun run verify --out _site`: passed with 82 tests / 272 assertions, Astro check across 44 files with 0 errors / 0 warnings / 0 hints, 2,969 Astro pages, 2,884 entry-metadata pages with zero issues, a final HTML audit across 5,864 routes, a generated URL audit across 6,037 unique URLs, and zero production vulnerabilities.
- Generated-surface drift passed with 1,482 API entries, fixed-timestamp byte identity, and zero docs/audit-artifact/local-path leakage.

### Known Notes

- Tooling fail-closes mechanical drift for a declared `canonical_anchor`, but deciding whether a page is semantically a mirror and checking the anchor-to-mirror reverse link remain editorial-review responsibilities.
- This is a documentation-contract repair; it does not reclassify existing entries' canonical designations or public facts.
- A GitHub Release is created or updated only when publishing `main`; this `pre` repair does not create one.

### Next Steps

- Confirm the real pre-push, remote HEAD, and PR checks; attach closeout evidence to Issue #190; and wait for maintainer review. Do not self-close the issue.

## 中文

### 发布范围

- Issue #190 的当前 `canonical_anchor` 契约、mirror policy、cross-domain anchor convention、开发文档入口、三语 README / CHANGELOG、本 release note，以及由 release generator 同步的公开 discovery surface。
- 不修改 wiki entry 事实正文、`ja` / `en` translation mirror、domain map 或公开 URL。

### 主要变更

- 统一 root schema、architecture docs 与 policy pages 的当前契约：真正的 mirror 必须声明 `canonical_anchor`，anchor 与普通 related page 必须省略；声明目标可解析、core-body link 与 drift zero 属于 release gate；mirror 的语义判断与 reverse link 仍由 editorial review 负责。
- 将实施前 proposal 明确为 historical decision record，并把当前 authority 集中到 `SCHEMA.md`、ADR-007 与 entry-authoring checklist。
- 把此前未列出的 11 份 active developer docs 加入 `docs/README.md`，使唯一开发文档入口可以到达全部 active docs；同时把 JapanFG 拆分前遗留的当前表述更新为 role-specific institution domains。

### 验证结果

- `bun run docs:audit`: 通过。
- `bun run docs:stale`: 66 份 active docs / 16 checks，stale reference 0，通过。
- `git diff --check`: 通过。
- `bun run verify --out _site`: 以 82 tests / 272 assertions、Astro check 44 files / 0 errors / 0 warnings / 0 hints、Astro 2,969 pages、entry metadata 2,884 / issues 0、final HTML audit 5,864 routes、generated URL audit 6,037 unique URLs、production vulnerability 0 通过。
- generated-surface drift 确认 API 1,482 entries 与 fixed-timestamp byte identity，docs / audit artifact / local path leakage 0，通过。

### 已知注意事项

- tooling 会对已声明 `canonical_anchor` 的机械 drift 执行 fail-close，但页面是否在语义上属于 mirror，以及 anchor 到 mirror 的 reverse link，仍由 editorial review 负责。
- 本次是 documentation-contract repair，不重新分类既有 entry 的 canonical designation，也不修改公开事实。
- GitHub Release 仅在发布 `main` 时创建或更新；本次面向 `pre` 的修复不会创建。

### 下一步

- 确认 real pre-push、remote HEAD 与 PR checks，把 closeout evidence 写入 Issue #190，并等待 maintainer review；不 self-close Issue。
