# 人間向けサイトのコンパクト読面

## 日本語

### 公開範囲

- Issue #287 の compact human-site read surface を公開する。
- corpus、品質バッジ、鮮度ラベル、typed `entity_edges` パネルは含めない。

### 主要変更

- ホームに領域の全量カバレッジマップを出す。
- 宣言済み `canonical_anchor` を同一主体グループとしてホーム、領域一覧、browse、条目ヘッダに出す。
- 領域一覧に既存の title + lead / TL;DR を載せる。

### 検証結果

- 前提条件: table-provenance residual 0、consistency 0、freshness overdue 0、`canonical_anchor_drift` 0。
- `bun run verify --out _site` PASS。
- visual QA: ja/en ホーム、browse、`megabanks/smbc`。

### 既知の注意点

- 既存の evidence strip（確度・更新・要再確認）は残す。本 unit はバッジを増やさない。
- typed relationship panel は Horizon 2 / #279 の範囲外のまま。

### 次の作業

- work→`pre`→`main`、tag `v2026.08.14-4`、#287 を close する。

## English

### Release Scope

- Ship the compact human-site read surface from Issue #287.
- Leave corpus edits, quality badges, freshness labels, and typed `entity_edges` panels out.

### Major Changes

- Show the full domain coverage map on home.
- Group declared `canonical_anchor` mirrors on home, domain indexes, browse, and the entry header.
- Add existing title + lead / TL;DR excerpts to domain lists.

### Validation Results

- Prerequisites: table-provenance residual 0, consistency 0, freshness overdue 0, `canonical_anchor_drift` 0.
- `bun run verify --out _site` PASS.
- Visual QA: ja/en home, browse, and `megabanks/smbc`.

### Known Notes

- The existing evidence strip stays. This unit does not add badges.
- Typed relationship panels remain out of scope.

### Next Steps

- Merge work→`pre`→`main`, tag `v2026.08.14-4`, and close #287.

## 中文

### 发布范围

- 发布 #287 的 compact human-site read surface。
- 不含 corpus 修改、质量徽章、新鲜度标签或 typed `entity_edges` 面板。

### 主要变更

- 首页展示完整领域覆盖图。
- 把已声明 `canonical_anchor` 的同一主体组显示在首页、领域页、browse 和条目头。
- 领域列表补上既有 title + lead / TL;DR。

### 验证结果

- 前置条件均为 0。`bun run verify --out _site` 通过。视觉验收覆盖 ja/en 首页、browse、SMBC mirror。

### 已知注意事项

- 保留现有 evidence strip，不新增徽章。
- typed relationship 面板仍不在本 unit。

### 下一步

- 合并 work→`pre`→`main`，tag `v2026.08.14-4`，关闭 #287。
