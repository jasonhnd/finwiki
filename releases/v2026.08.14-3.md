# 鮮度校正と実体ノード第一批

## 日本語

### 公開範囲

- Issue #278 の freshness 検出器校正、#279/#286 の megabank entity frontmatter 第一批、#280 の compact surface 仕様、#281 の API `canonical_anchor` 決定を同一 unit とする。
- UI 実装 (#287) は含めない。

### 主要変更

- `scenario-assumption` タグ付きシナリオ頁は、期限内かつ cadence 内で `low_confidence` だけの場合に freshness JSON から除外する。
- りそな銀行・埼玉りそな・日本郵政 HD・SMBC 重複ルートに公開一次資料付き entity メタデータを追加する。
- compact read surface と per-entry API 決定を docs に固定する。

### 検証結果

- `bun test tools/fact_freshness_audit.test.ts` 2 pass。
- `entity:audit` issues 0。wiki `canonical_anchor_drift` 0。`docs:audit` PASS。

### 既知の注意点

- SMBC の二重ルートは本 unit では統合削除しない。
- compact surface は仕様のみ。実装は #287。

### 次の作業

- work→`pre`→`main`、tag `v2026.08.14-3`、#278–#281 と #286 を close する。

## English

### Release Scope

- Ship #278 freshness calibration, #279/#286 megabank entity frontmatter batch 01, the #280 compact-surface spec, and the #281 API `canonical_anchor` decision.
- Leave UI implementation (#287) out of this unit.

### Major Changes

- Scenario pages tagged `scenario-assumption` drop out of freshness JSON when `low_confidence` is the only actionable reason and review/cadence windows still hold.
- Add public-source entity metadata on Resona Bank, Saitama Resona Bank, Japan Post Holdings, and the SMBC duplicate route.
- Record the compact read-surface spec and the keep-the-API-field decision.

### Validation Results

- Freshness unit tests: 2 pass.
- Entity audit issues 0. Wiki `canonical_anchor_drift` 0. `docs:audit` PASS.

### Known Notes

- The two SMBC routes are not merged in this unit.
- The compact surface is spec-only; implementation is #287.

### Next Steps

- Merge work→`pre`→`main`, tag `v2026.08.14-3`, and close #278–#281 and #286.