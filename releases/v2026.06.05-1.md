# P3 内容扩充 — 最薄の non-profit / trade を各 +6 entry（12 entry 追加）

## 日本語

### 公開範囲

- roadmap P3 の最薄 2 領域を並列 subagent で拡充: `non-profit` 6→12、`trade` 6→12（計 +12 公開情報 entry）。
- 各領域 `INDEX.md`、root `INDEX.md` ドメイン表（non-profit / trade の count + scope）、discovery 全再生成、三語 README/CHANGELOG/`releases/v2026.06.05-1.md`。

### 主要変更

- **`non-profit` +6**: 社会福祉法人 / 学校法人 / 医療法人 / 宗教法人 / 公益法人会計基準 / 助成財団（grant-making）。一般・公益財団/社団、NPO/認定NPO に加え、業種別免許法人と会計・助成レイヤーを補完。
- **`trade` +6**: NEXI 貿易保険 / 通関・関税・原産地 / FTA・EPA・RCEP / 電子提単（MLETR）/ フォーフェイティング・国際ファクタリング / サプライチェーンファイナンス。既存の L/C・荷為替・Incoterms・JETRO に貿易金融の周辺を追加。
- 2 並列 agent は file-scope 隔離（自領域のみ）+ 全 wikilink 実在検証。root INDEX の count は並列編集の race を Phase 2 で disk 実数へ照合修正（trade 6→12 を再適用）。

### 検証結果

- `bun tools/release.ts --check --strict`: **EXIT=0**、entries 1473→1485（+12）/ issues=0 / dead=0 / canonical_drift=0、md=1559 / domains=40、counts in sync、JSON/LF/dup-id OK。

### 既知の注意点 / 次の作業

- **P4（i18n）は未完**: 新 12 entry + v12 の 43 entry + 新 17 ドメイン INDEX は ja/zh/en mirror 未生成。i18n パイプライン（`site/scripts/translate.mjs`、Claude Haiku）は `ANTHROPIC_API_KEY` が必要で、現環境に未設定のため未実行。鍵設定後に `bun site/scripts/translate.mjs` で増量翻訳可能（mirror 欠如は graceful fallback で build は緑）。

## English

### Release Scope

- Expanded the two thinnest roadmap-P3 domains via parallel subagents: `non-profit` 6→12, `trade` 6→12 (+12 public-information entries).
- Their `INDEX.md` files, the root `INDEX.md` domain map (non-profit / trade count + scope), full discovery regen, trilingual README/CHANGELOG/`releases/v2026.06.05-1.md`.

### Major Changes

- **`non-profit` +6**: social-welfare / school / medical / religious corporations, the 公益法人会計基準 accounting layer, and grant-making (助成) foundations — complementing the existing general/public-interest foundations & NPO pages.
- **`trade` +6**: NEXI trade insurance, customs / tariff / origin, FTA / EPA / RCEP, electronic bill of lading (MLETR), forfaiting / international factoring, and supply-chain finance — adding the trade-finance surround to the existing L/C, documentary collection, Incoterms, and JETRO pages.
- The 2 agents ran under strict file-scope isolation; root-INDEX counts were reconciled to disk reality in a Phase-2 pass (re-applying trade 6→12 after a parallel-edit race).

### Validation Results

- `bun tools/release.ts --check --strict`: **EXIT=0**, entries 1473→1485 (+12) / issues=0 / dead=0 / canonical_drift=0, md=1559 / domains=40, counts in sync, JSON/LF/dup-id OK.

### Known Notes / Next Work

- **P4 (i18n) is NOT done**: the new 12 entries + the 43 v12 entries + the 17 new domain INDEX pages have no ja/zh/en mirrors. The i18n pipeline (`site/scripts/translate.mjs`, Claude Haiku) requires `ANTHROPIC_API_KEY`, which is unset in the current environment, so it was not run. With the key set, `bun site/scripts/translate.mjs` does the incremental translation (missing mirrors fall back gracefully, so the build stays green).

## 中文

### 发布范围

- 用并行 subagent 扩充 roadmap P3 最薄的两个域：`non-profit` 6→12、`trade` 6→12（+12 个公开信息 entry）。
- 各域 `INDEX.md`、root `INDEX.md` 域表（non-profit / trade 的 count + scope）、discovery 全重生成、三语 README/CHANGELOG/`releases/v2026.06.05-1.md`。

### 主要变更

- **`non-profit` +6**：社会福祉法人 / 学校法人 / 医疗法人 / 宗教法人 / 公益法人会计基准 / 助成财团——在原有的一般·公益财团/社团、NPO/认定NPO 之外，补齐业种免许法人与会计·助成层。
- **`trade` +6**：NEXI 贸易保险 / 通关·关税·原产地 / FTA·EPA·RCEP / 电子提单（MLETR）/ 福费廷·国际保理 / 供应链金融——在原有 L/C、跟单托收、Incoterms、JETRO 之外补齐贸易金融周边。
- 2 个 agent file-scope 隔离；root INDEX 的 count 在 Phase 2 按 disk 实数对账修正（并行编辑 race 后重新把 trade 6→12）。

### 验证结果

- `bun tools/release.ts --check --strict`：**EXIT=0**、entries 1473→1485（+12）/ issues=0 / dead=0 / canonical_drift=0、md=1559 / domains=40、counts in sync、JSON/LF/dup-id OK。

### 已知注意 / 下一步

- **P4（i18n）未完成**：新增 12 个 entry + v12 的 43 个 entry + 17 个新域 INDEX 都没有 ja/zh/en 镜像。i18n 流水线（`site/scripts/translate.mjs`，Claude Haiku）需要 `ANTHROPIC_API_KEY`，当前环境未设置，故未运行。设置密钥后用 `bun site/scripts/translate.mjs` 做增量翻译（镜像缺失会优雅降级，build 仍绿）。
