# package metadata の公開同期

## 日本語

### 公開範囲

- root `package.json` の公開 project metadata。
- README / CHANGELOG / release notes。
- release-generated discovery surface と root content map。
- GitHub Release metadata。

### 主要変更

- 前回 push 後に未コミットで残っていた root `package.json` を公開 repository state に取り込みました。
- `version`、日本語 description、`main`、`directories.lib`、repository URL、keywords / author、license、bugs URL、homepage を追加しました。
- README / CHANGELOG / release notes を三語で同期しました。
- release tooling で Markdown count、root `index.html`、AI discovery surface、footer timestamp を同期します。

### 検証結果

- release strict check、site build、Pagefind index、duplicate HTML id gate、diff whitespace check を push 前に再実行します。
- pre-push hook は `bun tools/release.ts --check --strict` を実行します。

### 既知の注意点

- 今回は metadata 同期のみです。
- wiki 本文、i18n entry、rendered site UI の挙動は変更しません。

### 次の作業

- push 後に remote HEAD、GitHub Actions、GitHub Release を確認します。
- working tree に未提交内容が残っていないことを確認します。

## English

### Release Scope

- Public project metadata in the root `package.json`.
- README / CHANGELOG / release notes.
- Release-generated discovery surfaces and root content map.
- GitHub Release metadata.

### Major Changes

- Committed the root `package.json` metadata that remained uncommitted after the previous push.
- Added version, Japanese description, `main`, `directories.lib`, repository URL, keywords / author, license, bugs URL, and homepage.
- Kept README / CHANGELOG / release notes synchronized in Japanese, English, and Chinese.
- Release tooling will resync Markdown counts, root `index.html`, AI discovery surfaces, and footer timestamp.

### Validation Results

- Release strict check, site build, Pagefind index, duplicate HTML id gate, and diff whitespace check are rerun before push.
- The pre-push hook runs `bun tools/release.ts --check --strict`.

### Known Notes

- This is a metadata sync only.
- No wiki body, i18n entry, or rendered site UI behavior changes are included.

### Next Work

- After push, verify remote HEAD, GitHub Actions, and GitHub Release.
- Confirm that no uncommitted work remains in the working tree.

## 中文

### 发布范围

- root `package.json` 的公开 project metadata。
- README / CHANGELOG / release notes。
- release-generated discovery surface 与 root content map。
- GitHub Release metadata。

### 主要变更

- 提交上一次 push 后仍未提交的 root `package.json` 元数据。
- 新增 version、日文 description、`main`、`directories.lib`、repository URL、keywords / author、license、bugs URL、homepage。
- 按日文、英文、中文同步 README / CHANGELOG / release notes。
- 通过 release tooling 重新同步 Markdown count、root `index.html`、AI discovery surface 和 footer timestamp。

### 验证结果

- push 前重新执行 release strict check、site build、Pagefind index、duplicate HTML id gate、diff whitespace check。
- pre-push hook 会执行 `bun tools/release.ts --check --strict`。

### 已知注意事项

- 本次只是 metadata 同步。
- 不修改 wiki 正文、i18n entry 或 rendered site UI 行为。

### 下一步

- push 后确认 remote HEAD、GitHub Actions 和 GitHub Release。
- 确认 working tree 不再有未提交内容。
