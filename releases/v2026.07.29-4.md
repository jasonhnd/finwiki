# エージェント基盤3件の公開状態更新

## 日本語

### 公開範囲

- Issue #220 の残存 fact-freshness queue を構成する Circle Agent Stack、Coinbase AgentKit / Agentic Wallets、Agent2Agent (A2A) の 3 entry を、2026-07-29 時点の live な公式一次情報で再検証する。
- 3 source entries、現在の ja / en mirrors、三言語 README / CHANGELOG / release state、root index と generated discovery surfaces を対象とする。
- `.opinions/**`、provenance queue、他の wiki entry は変更しない。

### 主要変更

- Circle の live product page / developer docs が Agent Wallets、Agent Marketplace、Circle CLI、Nanopayments、Circle Skills を公開 product surface として提供していることを確認した。
- Coinbase の live Agentic Wallet product / docs と public AgentKit repository が CLI / MCP / framework entry point を維持していることを確認した。現行 security architecture に合わせ title を `MPC agent wallets` から `TEE agent wallets` へ修正し、旧 title は alias として保持した。
- A2A の公式 specification が 1.0.0 を latest release とし、v1.0 が first stable / production-ready release、Linux Foundation が複数 industry の active production deployment と報告していることを確認した。
- 3 entry を `candidate` から `active`、`confidence` を `certain`、`last_updated` / `last_tended` を 2026-07-29、`review_by` を 2026-10-27 に更新し、本文と ja / en mirrors / source hashes を同期した。

### 検証結果

- exact `bun tools/fact_freshness_audit.ts --json --as-of 2026-07-29` は 3 actionable rows から `[]` となった。
- `bun run i18n:status -- --json` は ja / en 各 1,442 mirrors、current 1,442、stale / orphaned / missing 0。
- full canonical `bun run verify --out _site` は PASS。release docs 23 files / 21 strict notes / 76 grandfathered notes、wiki 1,489 entries / issues 0 / canonical drift 0、generated API 1,482 entries / fixed-timestamp byte identity / leakage 0、production vulnerability 0、Astro check 44 files / 0 errors / 0 warnings / 0 hints、89 tests / 310 assertions、Astro 2,969 pages、entry metadata 2,884 / issues 0、Pagefind 2,968 pages、assembly 6,258 Astro files + 3,080 raw files、13 required routes、final HTML 5,864 routes、generated URL 6,044 unique internal URLs、`git diff --check` を確認した。

### 既知の注意点

- `active` は現在の公式公開 lifecycle を示し、全機能の GA、利用規模、market share を意味しない。
- Coinbase Agentic Wallet MCP など個別 surface の maturity label は公式 documentation に従う。
- 3 entry は high-volatility topic として 90 日 cadence を維持し、次回 review は 2026-10-27。

### 次の作業

- source commit で canonical verification、`pre` / `main` PR checks、production deployment を通す。
- production と同じ main merge SHA に tag / GitHub Release `v2026.07.29-4` を作成する。
- Issue #220 に official-source classification、audit delta、translation / deployment evidence を記録して close し、親 Issue #192 と次の provenance child queue を更新する。

## English

### Release Scope

- Recheck the three entries forming Issue #220's residual fact-freshness queue—Circle Agent Stack, Coinbase AgentKit / Agentic Wallets, and Agent2Agent (A2A)—against live official primary sources as of 2026-07-29.
- Scope covers the three source entries, current ja/en mirrors, trilingual README/CHANGELOG/release state, root index, and generated discovery surfaces.
- `.opinions/**`, the provenance queue, and unrelated wiki entries remain unchanged.

### Major Changes

- Confirmed that Circle's live product page and developer docs expose Agent Wallets, Agent Marketplace, Circle CLI, Nanopayments, and Circle Skills as public product surfaces.
- Confirmed that Coinbase's live Agentic Wallet product/docs and public AgentKit repository retain the CLI, MCP, and framework entry points. Corrected the title from `MPC agent wallets` to `TEE agent wallets` to match the current security architecture while retaining the old title as an alias.
- Confirmed that the official A2A specification identifies 1.0.0 as latest, that v1.0 is the first stable, production-ready release, and that the Linux Foundation reports active production deployments across multiple industries.
- Moved all three entries from `candidate` to `active`, set `confidence: certain`, moved `last_updated` / `last_tended` to 2026-07-29 and `review_by` to 2026-10-27, and synchronized body evidence, ja/en mirrors, and source hashes.

### Validation Results

- Exact `bun tools/fact_freshness_audit.ts --json --as-of 2026-07-29` moved from three actionable rows to `[]`.
- `bun run i18n:status -- --json` reports 1,442 mirrors per language, all 1,442 current, with zero stale, orphaned, or missing mirrors.
- Full canonical `bun run verify --out _site` passed: 23 release-doc files / 21 strict notes / 76 grandfathered notes; 1,489 wiki entries / zero issues / zero canonical drift; 1,482 generated API entries / fixed-timestamp byte identity / zero leakage; zero production vulnerabilities; Astro check across 44 files with 0 errors / 0 warnings / 0 hints; 89 tests / 310 assertions; 2,969 Astro pages; 2,884 entry-metadata pages with zero issues; 2,968 Pagefind pages; assembly of 6,258 Astro files plus 3,080 raw files; all 13 required routes; 5,864 final HTML routes; 6,044 unique generated internal URLs; and `git diff --check`.

### Known Notes

- `active` identifies the current official public lifecycle; it does not mean every feature is GA or establish usage scale or market share.
- Maturity labels for individual surfaces such as Agentic Wallet MCP continue to follow official documentation.
- All three entries retain a 90-day cadence as high-volatility topics, with the next review on 2026-10-27.

### Next Steps

- Pass canonical verification on the source commit, both `pre` / `main` PR checks, and production deployment.
- Create tag/GitHub Release `v2026.07.29-4` on the same main merge SHA as production.
- Close Issue #220 with official-source classification, audit delta, translation/deployment evidence, then update parent #192 and the next provenance child queue.

## 中文

### 发布范围

- 用截至 2026-07-29 仍可访问的官方一手资料，复核组成 Issue #220 剩余 fact-freshness queue 的 3 个条目：Circle Agent Stack、Coinbase AgentKit / Agentic Wallets 与 Agent2Agent (A2A)。
- 范围包括 3 个 source entries、当前 ja / en mirrors、三语 README / CHANGELOG / release state、root index 与 generated discovery surfaces。
- 不修改 `.opinions/**`、provenance queue 或其他 wiki entries。

### 主要变更

- 确认 Circle 的 live product page 与 developer docs 把 Agent Wallets、Agent Marketplace、Circle CLI、Nanopayments 与 Circle Skills 作为公开 product surfaces 提供。
- 确认 Coinbase 的 live Agentic Wallet product/docs 与 public AgentKit repository 仍保留 CLI、MCP 与 framework 入口。按照当前 security architecture，把 title 从 `MPC agent wallets` 修正为 `TEE agent wallets`，同时保留旧 title 作为 alias。
- 确认 A2A 官方 specification 将 1.0.0 标为 latest，v1.0 是首个 stable / production-ready release，Linux Foundation 也报告多个行业已有 active production deployments。
- 把 3 个条目从 `candidate` 更新为 `active`，设为 `confidence: certain`，将 `last_updated` / `last_tended` 更新到 2026-07-29、`review_by` 更新到 2026-10-27，并同步正文证据、ja / en mirrors 与 source hashes。

### 验证结果

- 精确 `bun tools/fact_freshness_audit.ts --json --as-of 2026-07-29` 从 3 条 actionable rows 降为 `[]`。
- `bun run i18n:status -- --json` 显示 ja / en 各 1,442 mirrors，current 1,442，stale / orphaned / missing 均为 0。
- full canonical `bun run verify --out _site` 已通过：release docs 23 files / 21 strict notes / 76 grandfathered notes、wiki 1,489 entries / issues 0 / canonical drift 0、generated API 1,482 entries / fixed-timestamp byte identity / leakage 0、production vulnerability 0、Astro check 44 files / 0 errors / 0 warnings / 0 hints、89 tests / 310 assertions、Astro 2,969 pages、entry metadata 2,884 / issues 0、Pagefind 2,968 pages、assembly 6,258 个 Astro files + 3,080 个 raw files、13 个 required routes、final HTML 5,864 routes、generated URL 6,044 个 unique internal URLs 与 `git diff --check`。

### 已知注意事项

- `active` 表示当前官方公开 lifecycle，并不意味着每项功能都已 GA，也不证明使用规模或 market share。
- Agentic Wallet MCP 等单独 surface 的 maturity label 仍以官方 documentation 为准。
- 3 个条目作为 high-volatility topics 继续采用 90 天 cadence，下次 review 是 2026-10-27。

### 下一步

- 在 source commit 上通过 canonical verification、`pre` / `main` PR checks 与 production deployment。
- 在与 production 相同的 main merge SHA 上创建 tag / GitHub Release `v2026.07.29-4`。
- 向 Issue #220 记录 official-source classification、audit delta、translation / deployment evidence 后关闭，并更新父 Issue #192 与下一个 provenance child queue。
