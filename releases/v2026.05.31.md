# GPT i18n 翻訳バッチ安全収集・483/483 進捗

## 日本語

### 概要

2026-05-31 JST の i18n 翻訳バッチです。ユーザー指示に従い、Sonnet ではなく GPT/Codex worker で `site/src/content/i18n/{zh,en}/` の増分機械翻訳を進めました。

### 公開範囲

- 対象: `site/src/content/i18n/{zh,en}/`
- 補助記録: `README.md`、`CHANGELOG.md`、本 release notes
- 準備 job: 130 件（`.cache/jobs/w0..w9/`）
- 保持結果: placeholder 検証を通過した 132 language outputs
- 累計: zh 483 / en 483

### 主要変更

- `bun scripts/prep-parallel.mjs --workers 10 --size 13` で 130 件を準備。
- GPT/Codex worker で worker directory 単位に翻訳。
- `bun scripts/commit-translate.mjs` で placeholder 検証 + unmask を実行。
- 本バッチの `source_hash` と `fidelity: needs_review` が一致する 106 language outputs は削除し、次回増分で再処理できる状態に戻した。
- README の三語セクションに i18n 進捗を追記。

### 検証結果

- `bun run build`: pass、4147 pages built。
- `python tools/release.py --check --strict`: pass。
- link audit: entries=1411、issues=0。
- canonical counts: README / index.html と同期済み。

### 既知の注意点

- GPT worker の一部出力は placeholder を保持できず `needs_review` になったため、公開訳文としては保持しなかった。
- Astro build は `zh/fintech/japan-epi-three-types-overview` の duplicate id warning を出したが、frontmatter は UTF-8 で正常に閉じており build は成功した。
- 削除済み `needs_review` と missing は、次回の `prep-parallel` で再取得される。

### 次の作業

- 次バッチで `needs_review` / missing を再翻訳する。
- GPT worker prompt に実際の placeholder 表記（`笶ｰ...笶ｱ`）の byte-for-byte 保持をさらに強く入れる。
- duplicate id warning の原因を別途確認する。

## English

### Overview

This is the 2026-05-31 JST i18n translation batch. Per user instruction, the batch used GPT/Codex workers rather than Sonnet to continue incremental machine translation under `site/src/content/i18n/{zh,en}/`.

### Release Scope

- Target: `site/src/content/i18n/{zh,en}/`
- Supporting records: `README.md`, `CHANGELOG.md`, and this release note
- Prepared jobs: 130 (`.cache/jobs/w0..w9/`)
- Retained result: 132 placeholder-verified language outputs
- Cumulative progress: zh 483 / en 483

### Major Changes

- Prepared 130 jobs with `bun scripts/prep-parallel.mjs --workers 10 --size 13`.
- Ran GPT/Codex workers by worker directory.
- Ran placeholder verification and unmasking with `bun scripts/commit-translate.mjs`.
- Removed this batch's 106 `needs_review` language outputs by `source_hash`, so failed placeholder translations return to the incremental queue.
- Added trilingual README notes for the current i18n progress.

### Validation Results

- `bun run build`: passed, 4147 pages built.
- `python tools/release.py --check --strict`: passed.
- Link audit: entries=1411, issues=0.
- Canonical counts remain synchronized with README / index.html.

### Known Notes

- Some GPT worker outputs failed placeholder preservation and were marked `needs_review`; those files were not retained as public translations.
- Astro emitted a duplicate id warning for `zh/fintech/japan-epi-three-types-overview`, but the target frontmatter is valid UTF-8 and the build completed.
- Removed `needs_review` outputs and missing outputs will be picked up by the next `prep-parallel` run.

### Next Work

- Re-run the failed `needs_review` / missing entries in the next batch.
- Strengthen the GPT worker prompt around byte-for-byte preservation of the actual placeholder form (`笶ｰ...笶ｱ`).
- Investigate the duplicate id warning separately.