# GPT i18n 翻訳バッチ継続・1273/1273 進捗

## 日本語

### 概要

2026-06-01 JST の GPT/Codex i18n 継続バッチです。zh/en 1143 release 後、次の 130 source entries を GPT/Codex worker で処理し、累計進捗を zh 1273 / en 1273 に進めました。

### 公開範囲

- 対象: `site/src/content/i18n/{zh,en}/`
- 作業 batch: 130 source entries, 260 language outputs
- 記録更新: `README.md`, `CHANGELOG.md`, this release note
- AI discovery / counts: `tools/release.py --write` により同期

### 主要変更

- `bun scripts/prep-parallel.mjs --workers 10 --size 13` で 130 jobs を準備しました。
- 10 本の GPT/Codex workers で `.cache/jobs/w0..w9/` を処理しました。
- w1 / w4 / w6 / w7 の不足分を recovery workers に分割し、追加で single-file recovery を実行しました。
- w6 に残った worker 内部 `.translation-cache.json` を削除し、job metadata 誤認を解消しました。
- `policy-finance/jfc-sme-division-operating-model.md` と `policy-finance/okinawa-development-finance-corp.md` の zh output で mojibake した placeholder を `❰id❱` 形式へ機械的に復元し、source placeholder sequence と一致することを確認しました。
- 完了済み worker / recovery agents は archive 済みです。

### 検証結果

- `bun scripts/commit-translate.mjs`: ok=260, needs_review=0, missing=0.
- Cumulative i18n count: zh 1273 / en 1273.
- `bun run build`: passed, 4147 pages built.
- `python tools/release.py --check --strict`: passed in this release cycle.

### 既知の注意点

- Astro build 中に duplicate id warning は出ますが、今回の公開を止める error ではありません。
- 残りは約 107 source entries です。

### 次の作業

- 最終 batch で残り entries を GPT/Codex worker により翻訳します。
- `prep-parallel` が 0 jobs を返すまで、同じ verify-first flow を継続します。

## English

### Overview

This is the 2026-06-01 JST GPT/Codex i18n continuation batch. After the zh/en 1143 release, the next 130 source entries were processed with GPT/Codex workers, moving cumulative progress to zh 1273 / en 1273.

### Release Scope

- Target: `site/src/content/i18n/{zh,en}/`
- Batch: 130 source entries, 260 language outputs
- Records: `README.md`, `CHANGELOG.md`, and this release note
- AI discovery / counts: synchronized via `tools/release.py --write`

### Major Changes

- Prepared 130 jobs with `bun scripts/prep-parallel.mjs --workers 10 --size 13`.
- Ran 10 GPT/Codex workers over `.cache/jobs/w0..w9/`.
- Split the w1 / w4 / w6 / w7 gaps into recovery workers, plus one single-file recovery.
- Removed one stray worker `.translation-cache.json` from w6 so it could not be mistaken for job metadata.
- Mechanically restored mojibake placeholder tokens in the zh outputs for `policy-finance/jfc-sme-division-operating-model.md` and `policy-finance/okinawa-development-finance-corp.md`, then verified them against the source placeholder sequence.
- Archived completed worker / recovery agents.

### Validation Results

- `bun scripts/commit-translate.mjs`: ok=260, needs_review=0, missing=0.
- Cumulative i18n count: zh 1273 / en 1273.
- `bun run build`: passed, 4147 pages built.
- `python tools/release.py --check --strict`: passed in this release cycle.

### Known Notes

- Astro build still emits duplicate id warnings, but they are not release-blocking errors for this batch.
- About 107 source entries remain.

### Next Work

- Translate the remaining entries with the final GPT/Codex batch.
- Continue the same verify-first flow until `prep-parallel` reports 0 jobs.