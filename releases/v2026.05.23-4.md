# AI 発見性サーフェス追加 2026-05-23

## 日本語

### 概要

FinWiki の homepage は人間向けに読みやすく保ちつつ、AI / crawler が全公開 content を安定して発見・巡回できる machine-readable entry points を追加しました。今後 wiki content、indexes、domain counts、public snapshots を更新するたびに、同じ作業内で AI discovery files も再生成します。

### 対象範囲

- `robots.txt`
- `sitemap.xml`
- `llms.txt`
- `llms-full.txt`
- `ai-index.json`
- `tools/generate_ai_discovery.py`
- `index.html`
- `INDEX.md`
- `README.md`
- `AGENTS.md`
- `CHANGELOG.md`
- `releases/v2026.05.23-4.md`

### 主要変更

- `tools/generate_ai_discovery.py` を追加し、Markdown corpus、root `INDEX.md` domain map、`wiki-link-improvement-plan.md` audit summary から AI discovery surfaces を生成できるようにしました。
- `robots.txt` と `sitemap.xml` を追加し、crawler / search engine / AI browser が public site を発見しやすい状態にしました。
- `llms.txt`、`llms-full.txt`、`ai-index.json` を追加し、AI が entry points、URL rules、domain routes、page URLs、source paths、summaries、headings、resolved wikilinks を機械的に読めるようにしました。
- `INDEX.md` の domain routes に ordinary Markdown web links を併記し、Obsidian wikilinks を理解しない crawler でも core routes を追いやすくしました。
- `README.md`、`AGENTS.md`、homepage に、AI discovery files を future content updates と同期する maintenance rule を追加しました。

### 数値スナップショット

- Markdown files: 910
- Public Markdown pages in machine manifest: 909
- Sitemap URLs: 910
- Topical domains: 20
- Link-audited entries: 881
- Unresolved link issues: 0
- Text volume: 約364.6万字 / 3,646,435 non-space UTF-8 characters
- Word-like tokens: 571,814

### 検証結果

- `python3 tools/generate_ai_discovery.py` を実行し、AI discovery files を生成。
- `python3 tools/wiki_link_audit.py --report wiki-link-improvement-plan.md --fail-on-issues` pass。
- `git diff --check` pass。
- `ai-index.json` JSON parse、sitemap URL count、`llms.txt` / `llms-full.txt` / `robots.txt` の key phrase scan を実行。

### 既知の注意点

- 本 release は discovery / traversal layer の追加です。既存 wiki 本文の factual content は変更していません。
- `llms-full.txt` は full manifest であり、各 page の本文全文ではなく、URL、source path、summary、headings、resolved wikilinks を列挙します。

### 次の作業

- GitHub Pages 反映後に、`/robots.txt`、`/sitemap.xml`、`/llms.txt`、`/llms-full.txt`、`/ai-index.json` が 200 で取得できることを live site で確認します。

## English

### Overview

FinWiki now keeps the homepage readable for humans while adding machine-readable entry points that let AI agents and crawlers discover and traverse the full public corpus. Future wiki content, index, domain-count, or public-snapshot updates must regenerate the AI discovery files in the same work.

### Scope

- `robots.txt`
- `sitemap.xml`
- `llms.txt`
- `llms-full.txt`
- `ai-index.json`
- `tools/generate_ai_discovery.py`
- `index.html`
- `INDEX.md`
- `README.md`
- `AGENTS.md`
- `CHANGELOG.md`
- `releases/v2026.05.23-4.md`

### Major Changes

- Added `tools/generate_ai_discovery.py` to generate AI discovery surfaces from the Markdown corpus, root `INDEX.md` domain map, and `wiki-link-improvement-plan.md` audit summary.
- Added `robots.txt` and `sitemap.xml` so crawlers, search engines, and AI browsers can discover the public site.
- Added `llms.txt`, `llms-full.txt`, and `ai-index.json` so AI readers can consume entry points, URL rules, domain routes, page URLs, source paths, summaries, headings, and resolved wikilinks.
- Added ordinary Markdown web links beside the core Obsidian wikilinks in `INDEX.md`, making domain routes easier for crawlers that do not understand wikilinks.
- Updated `README.md`, `AGENTS.md`, and the homepage with a maintenance rule requiring AI discovery files to stay synchronized with future content updates.

### Numeric Snapshot

- Markdown files: 910
- Public Markdown pages in machine manifest: 909
- Sitemap URLs: 910
- Topical domains: 20
- Link-audited entries: 881
- Unresolved link issues: 0
- Text volume: ~3.65M chars / 3,646,435 non-space UTF-8 characters
- Word-like tokens: 571,814

### Validation

- Ran `python3 tools/generate_ai_discovery.py` and generated the AI discovery files.
- `python3 tools/wiki_link_audit.py --report wiki-link-improvement-plan.md --fail-on-issues` passed.
- `git diff --check` passed.
- Parsed `ai-index.json`, checked sitemap URL count, and scanned key phrases in `llms.txt`, `llms-full.txt`, and `robots.txt`.

### Known Notes

- This release adds the discovery / traversal layer. It does not change factual content inside existing wiki body pages.
- `llms-full.txt` is a full manifest, not a full-text dump; it lists page URLs, source paths, summaries, headings, and resolved wikilinks.

### Next Work

- After GitHub Pages deployment, verify that `/robots.txt`, `/sitemap.xml`, `/llms.txt`, `/llms-full.txt`, and `/ai-index.json` return 200 on the live site.