# 依存関係の修正と三円シナリオ再確認

## 日本語

### 公開範囲

- Issue #276 の Astro 経由 `nanoid` / `js-yaml` high を overrides で解消する。
- Issue #277 のうち `fintech/three-circles-mra-2030-economic-scale.md` 1 件を公開一次資料で再確認する。
- 残りの freshness 80 件と #278–#281 は本 release に含めない。

### 主要変更

- `site/package.json` に `nanoid@3.3.18` と `js-yaml@4.3.1` の overrides を追加し lockfile を更新した。
- 三円シナリオ頁は GENIUS Act S.1582 / Public Law 119-27 を再確認し、シナリオ数値を公式予測へ昇格させず、`scenario-assumption` tag と `review_by=2026-11-12` を設定した。

### 検証結果

- `cd site && bun audit --production` は 0 vulnerabilities。
- 当該 1 頁の ja/en `source_hash` を再計算した。

### 既知の注意点

- freshness 期限切れ 81 件の残りは Issue #277。
- `confidence: possible` のシナリオ頁は #278 で検出器校正するまで default JSON に残り得る。

### 次の作業

- work→`pre`→`main` のあと same-SHA tag `v2026.08.14`。
- Issue #276 を close し、#277 を残件 inventory で継続する。

## English

### Release Scope

- Clear Issue #276 by overriding the Astro-chain `nanoid` and `js-yaml` highs.
- Recheck one Issue #277 page: `fintech/three-circles-mra-2030-economic-scale.md`.
- Leave the other overdue freshness rows and Issues #278–#281 out of this release.

### Major Changes

- Added `site/package.json` overrides for `nanoid@3.3.18` and `js-yaml@4.3.1` and refreshed the lockfile.
- Rechecked GENIUS Act S.1582 / Public Law 119-27, kept the $130B / $384B figures as scenario inputs, added `scenario-assumption`, and set `review_by=2026-11-12`.

### Validation Results

- `cd site && bun audit --production` reports 0 vulnerabilities.
- ja/en `source_hash` values for the one page were recomputed.

### Known Notes

- The remaining overdue freshness rows stay on Issue #277.
- Scenario pages with `confidence: possible` may still appear in default freshness JSON until Issue #278.

### Next Steps

- Merge work→`pre`→`main` and tag `v2026.08.14` on the exact production SHA.
- Close #276 and continue #277 against the remaining inventory.

## 中文

### 发布范围

- 通过 overrides 关闭 Issue #276 的 Astro 链 `nanoid` / `js-yaml` high。
- 复核 Issue #277 中的 `fintech/three-circles-mra-2030-economic-scale.md` 一页。
- 其余 freshness 行与 #278–#281 不纳入本 release。

### 主要变更

- 在 `site/package.json` 增加 `nanoid@3.3.18` 与 `js-yaml@4.3.1` overrides 并更新 lockfile。
- 复核 GENIUS Act S.1582 / Public Law 119-27，情景数字不升格为官方预测，添加 `scenario-assumption`，`review_by=2026-11-12`。

### 验证结果

- `cd site && bun audit --production` 为 0。
- 该页 ja/en `source_hash` 已重算。

### 已知注意事项

- 其余过期 freshness 仍在 Issue #277。
- `confidence: possible` 情景页在 #278 完成前仍可能出现在 default JSON。

### 下一步

- work→`pre`→`main` 后在 exact SHA 打 tag `v2026.08.14`。
- 关闭 #276，按 inventory 继续 #277。
